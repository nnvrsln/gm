/**
 * Схема базы. Пока пуста — таблицы приходят следующим шагом.
 *
 * Что здесь появится и почему именно так — `docs/tz/06-BACKEND.md`, раздел
 * «Схема БД»: `orders` (с `total` и `paid`, потому что бронь за 10 000 ₽ —
 * это двухэтапная оплата), `payments`, `payment_events` с уникальным
 * `event_key` под идемпотентность вебхука и `consents` отдельной
 * неизменяемой таблицей под 152-ФЗ.
 *
 * Черновик прежней схемы этого же проекта поднимается из git:
 * `git show 37c81ea:api/_lib/schema.ts`.
 *
 * Файл существует уже сейчас, потому что на него ссылаются подключение к
 * базе и drizzle-kit: пустой он ничего не ломает, отсутствующий — ломает
 * обоих.
 */

export {}
