import type { CSSProperties } from 'react'
import coachCheer from '../assets/coach-cheer.webp'
import { QUOTE } from '../data/quote'

/**
 * Слайд 9 ТЗ «Мотивационная цитата и жизнеутверждающее фото» — последняя
 * секция страницы.
 *
 * ── Что задано ───────────────────────────────────────────────────────────
 * Заказчик (06.09.2026) дал фразу и снимок. Текст дословно в `data/quote.ts`.
 *
 * ── Про кадр ─────────────────────────────────────────────────────────────
 * Исходный снимок был студийный, фигура на белом фоне, и это была главная
 * трудность подачи: на тёмной странице белый прямоугольник читался вставкой.
 * Перебрано и отклонено владельцем: затемнять самого ГМ, растворять кадр
 * овальной маской, ставить плашкой со скруглением, класть в световую панель
 * под цвет студийного фона.
 *
 * 06.09.2026 владелец прислал кадр **с вырезанным фоном**, и всё это стало
 * не нужно. Фигура лежит прямо на странице: ни панели, ни рамки, ни маски,
 * ни фильтров.
 *
 * Ассет: `coach-cheer.webp`, 760×1466, WebP с альфой, 179 КБ — из PNG в
 * 3.3 МБ (оригинал в `docs/source/`). Прозрачные поля обрезаны, поэтому
 * края файла совпадают с габаритами фигуры и положение задаётся прямо.
 *
 * ── Композиция ───────────────────────────────────────────────────────────
 * Подача «крупный план», выбор владельца из десяти вариаций на макетной
 * `/quote.html`. Фигура увеличена сильно — 640px против 500px высоты секции
 * — и уходит за нижний край и за правый обрез: на последнем экране страницы
 * человек должен быть крупным, а не иллюстрацией к тексту. Правый вылет
 * нужен, чтобы у фигуры не было вертикальной границы, иначе вырезка снова
 * читается наклейкой.
 *
 * Цитата лежит внизу поверх корпуса — её держит шторка, поднимающаяся от
 * низа секции. Шторка заодно гасит низ в тон подвала, поэтому отдельный
 * сход в `bg-pitch` этой секции больше не нужен.
 *
 * ── Переход от FAQ ───────────────────────────────────────────────────────
 * Сверху волосяная линия — ровно та, что делит вопросы в аккордеоне выше
 * (`white/[.07]`, тот же отступ `px-5`). Ею список закрывается: без неё FAQ
 * обрывался в воздухе, а следом без предупреждения начиналась фигура.
 *
 * ── Типографика ──────────────────────────────────────────────────────────
 * Цитата целиком набрана антиквой одним кеглем — решение владельца. Раньше
 * фраза резалась по двоеточию: главная мысль антиквой, хвост мелким Onest.
 * Здесь это делило одну реплику одного человека на два голоса, поэтому
 * снято.
 *
 * Georgia — та же антиква, которой набраны цитаты в «Признании коллег»,
 * поэтому оба блока прямой речи на странице говорят одним голосом.
 *
 * Подпись — одна строка одного кегля («Гаджи Гаджиев, автор обучения»),
 * правка заказчика от 11.09.2026. Bebas из этой секции ушёл вместе с
 * прежней крупной фамилией.
 *
 * Переносы обязательны: строка идёт поверх фигуры, и «совершенствоваться» в
 * неё целиком не встаёт. `lang="ru"` стоит на `<html>`.
 *
 * ── Как секция сидит на странице ─────────────────────────────────────────
 * Своего тона нет: под ней та же база `CourseBackdrop`, `z-10` поднимает
 * содержимое над слоем. `px-5` на самой секции нет — иначе фигура не дойдёт
 * до края экрана; отступы держит текстовый блок.
 */

/** Антиква прямой речи — та же, что у цитат в «Признании коллег». */
const SERIF: CSSProperties = { fontFamily: 'Georgia, "Times New Roman", "Noto Serif", serif' }

export function QuoteSection() {
  return (
    <section id="quote" aria-label="Цитата автора обучения" className="relative z-10 overflow-hidden">
      {/* Линия, закрывающая список FAQ: та же, что делит вопросы в
          аккордеоне, и тем же отступом. */}
      <span aria-hidden="true" className="absolute inset-x-5 top-0 block h-px bg-white/[.07]" />

      {/* Фигура. Ни рамки, ни маски, ни фильтров: кадр показан как есть. */}
      <img
        src={coachCheer}
        alt="Гаджи Гаджиев"
        loading="lazy"
        width={760}
        height={1466}
        className="final-figure pointer-events-none absolute right-[-16%] top-9 h-[640px] w-auto max-w-none select-none"
      />

      {/* Шторка: держит читаемость цитаты поверх корпуса и гасит низ секции
          в тон подвала. */}
      <span
        aria-hidden="true"
        className="pointer-events-none absolute inset-x-0 bottom-0 h-72 bg-[linear-gradient(180deg,transparent_0%,rgba(8,13,20,.86)_46%,#05080b_100%)]"
      />

      <figure className="relative flex min-h-[500px] flex-col justify-end px-5 pb-10 pt-[196px]">
        <blockquote className="max-w-[88%]">
          {/* Кавычку рисует вёрстка, в тексте её нет: иначе она попадёт в
              копипаст и в микроразметку, когда та появится. Синяя — как у
              цитат в «Признании коллег». */}
          <p
            style={{ ...SERIF, hyphens: 'auto', WebkitHyphens: 'auto' } as CSSProperties}
            className="final-quote text-[20px] leading-[1.32] tracking-[-.02em] text-[#F2F6FB]"
          >
            <span aria-hidden="true" className="text-[#6AA0FF]">
              «
            </span>
            {QUOTE.text}
          </p>
        </blockquote>

        {/* Правка от 11.09.2026: подпись стала одной строкой одного кегля.
            Была пара «ГАДЖИЕВ» Bebas'ом в 24px + мелкий хвост «Гаджи
            Муслимович, автор обучения» — заказчик попросил ровный текст,
            имя и роль в одном голосе. Кегль взят мелкий: подпись под
            прямой речью не должна спорить с самой репликой. */}
        <figcaption className="mt-7 text-[14px] leading-[1.4] text-[#A9B6C6]">{QUOTE.signature}</figcaption>
      </figure>
    </section>
  )
}
