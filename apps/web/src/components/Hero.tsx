import heroPhoto from '../assets/hero-gadzhiev.webp'
import signature from '../assets/signature.webp'

export function Hero() {
  return (
    // Низ берётся из .section-rhythm (32px): у кнопок свечение слабое
    // (блюр 20px против прежних 30px) и успевает погаснуть до стыка, не
    // подкрашивая чёрный синим. Верх свой — это первый экран, а не рядовая
    // секция, и 168px отмеряют место под шапку кадра — на 36px больше
    // исходных 132px, чтобы текстовый блок не залезал на лицо ГМ в кадре.
    // Фото не трогаем: img внутри абсолютно спозиционирован и от этого
    // отступа не зависит, сдвигается только контент в потоке.
    <section className="section-rhythm relative isolate overflow-hidden pt-[168px]">
      {/* Снимок альбомный (1337×1176), контейнер узкий — кадр увеличен до 178%
          и сдвинут влево, чтобы в окно попали фигура справа, прожектор в правом
          верхнем углу и газон снизу, а левая половина осталась под текст. */}
      <img
        src={heroPhoto}
        alt="Гаджи Гаджиев на стадионе"
        className="photo-sharp-down pointer-events-none absolute -top-[10px] left-[-6%] z-0 w-[112%] max-w-none select-none brightness-[1.16] saturate-[1.06]"
      />
      {/* Размытая копия того же кадра: подхватывает низ там, где резкая уже
          погасла. filter применяется до маски, поэтому blur не «вытекает». */}
      <img
        src={heroPhoto}
        alt=""
        aria-hidden="true"
        className="photo-blur-down pointer-events-none absolute -top-[10px] left-[-6%] z-0 w-[112%] max-w-none select-none brightness-[1.16] saturate-[1.06]"
      />

      {/* Затемнение слева — под заголовок, снизу — стык со следующей секцией.
          Держим его слабым: фон должен читаться стадионом, а не чёрной плашкой. */}
      <div className="absolute inset-0 z-[1] bg-[linear-gradient(100deg,rgba(5,8,11,.74)_0%,rgba(5,8,11,.26)_56%,transparent_94%)]" />
      {/* Фото гаснет ДО конца секции — последние ~35px это ровная полоса,
          и именно эта пауза делает границу со следующим блоком читаемой.
          Но гаснет уже не в чистый чёрный: последний стоп переведён с
          #000206 (яркость 1.8) на #070d15 (12.3). Пока «Для кого» сама
          лежала на чёрном, разницы не было видно; когда секция ожила, эта
          полоса осталась ямой между двумя светлыми блоками — замер по стыку
          давал 13.7 у героя, 1.8 в полосе и 10.2 в секции, то есть темнее
          обоих соседей. Теперь герой приходит к стыку тем же тоном, каким
          секция от него уходит. Пауза сохранилась: она держится на разнице с
          фотографией выше, а не на чёрном. */}
      <div className="absolute inset-x-0 bottom-0 z-[2] h-60 bg-[linear-gradient(180deg,transparent_0%,rgba(3,5,8,.32)_46%,rgba(2,5,9,.84)_84%,#070d15_100%)]" />

      <div className="relative z-10 px-5">
        {/* ── Надзаголовок ──────────────────────────────────────────────────
            Правка от 11.09.2026: «Онлайн-академия футбольных тренеров Гаджи
            Гаджиева» снята, на её место переехала строка, которая раньше
            стояла подзаголовком под H1 (сам подзаголовок при этом убран —
            иначе текст дублировался бы на одном экране). Длина почти та же
            (57 знаков против 52), кегль и трекинг не трогаем. */}
        <p className="max-w-[300px] font-badge text-[11px] font-bold uppercase leading-[1.4] tracking-[.04em] text-white/58">
          Практическая система подготовки от{' '}
          {/* Имя выделено без нового цвета (красный не используем нигде на
              странице) — просто полная непрозрачность и вес жирнее общего
              текста строки, тот же приём, что делает разницу между title и
              описанием в остальных секциях. */}
          <span className="font-extrabold text-white">Гаджиева Гаджи Муслимовича</span>
        </p>
        {/* Настоящий автограф Гаджиева: заказчик прислал скан 11.09.2026
            (docs/podpis.png), он закрывает Q1/Q2 — набранная латиница
            «G. Gadzhiev» шрифтом Autograf снята вместе с самим шрифтом.
            Файл белый на прозрачном фоне и обрезан по штриху, поэтому
            красится не цветом, а прозрачностью, и позиционируется по
            своим краям без поправки на поля. */}
        <img
          src={signature}
          alt="Подпись Гаджи Гаджиева"
          className="signature mt-3 block w-[152px] select-none"
        />

        {/* ── Заголовок ──────────────────────────────────────────────────── */}
        {/* Правка от 06.09.2026: новое продуктовое обещание длиннее прежнего
            больше чем вдвое (180 знаков против 88) — это уже не короткий
            слоган, а развёрнутое предложение. Прежний приём (ручные переносы
            по фразовым группам, каждая своей строкой) на таком объёме давал
            бы 10+ рваных строк и плакатом уже не читался бы. Вместо этого
            текст отдан на автоперенос браузером, только крупнее надзаголовка
            и капслоком, чтобы иерархия «обещание крупнее пояснения» не
            потерялась. Кегль снижен до 22px
            (с 32px) и leading ослаблен до 1.18 — на плотном 0.96 многострочный
            абзац капслоком слипался. */}
        <h1 className="hero-title mt-5 text-[22px] uppercase leading-[1.18] tracking-title">
          {/* Ударение над «У» — просьба заказчика от 11.09.2026: знаком
              ударения, не капслоком. Комбинирующего U+0301 в тексте нет
              намеренно: в Bebas Neue Cyrillic этого знака нет, и браузер
              рендерил фолбэком весь кластер — буква выпадала из шрифта
              заголовка чужой формой и шириной. Знак рисуется псевдоэлементом
              .stress-acute (index.css), буква остаётся своей. */}
          <span>
            На<span className="stress-acute">у</span>читесь выстраивать системную подготовку команды — от плана на сезон до
            отдельного занятия, управлять нагрузкой и корректировать работу с учётом
            состояния игроков и задач команды
          </span>
        </h1>

        {/* ── Кнопка ─────────────────────────────────────────────────────── */}
        {/* ТЗ: кнопка одна, ведёт на программу. Была пара — вторая (btn-hero-secondary)
            снята, оставшаяся растянута на всю ширину: половина строки без соседа
            читалась бы обрубком. */}
        <div className="mt-9 flex">
          <a href="#program" className="btn-hero btn-hero-primary w-full">
            Узнать программу
          </a>
        </div>
      </div>
    </section>
  )
}
