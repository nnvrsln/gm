# Состояние работ — читать первым в новой сессии

Этот файл — точка входа. Работа идёт **по одному слайду за сессию**, каждый слайд
начинается с чистого диалога. Здесь лежит всё, что нужно, чтобы подхватить работу
без пересказа.

**Обновлять этот файл в конце каждой сессии.** Если он врёт, следующая сессия
начнётся с ошибки.

---

## Как начать сессию

1. Прочитать этот файл целиком.
2. Прочитать `docs/tz/01-SPEC.md` — **только раздел того слайда, над которым
   работаем.** Не весь файл, он большой.
3. Заглянуть в `docs/tz/03-QUESTIONS.md` — там вопросы по этому слайду с готовыми
   предложениями.
4. Запустить `npm run dev` → http://localhost:5173/
5. Работать. В конце — обновить раздел «Журнал» и «Текущий слайд» здесь.

Чего **не** надо делать в начале сессии: перечитывать `docs/HANDOFF.md` целиком,
пересматривать всё дерево `src`, заново разбирать `tz.docx`. Всё уже разобрано.

---

## Текущий слайд

> **Слайд 9 — «Финальная цитата». Секция была собрана в выбранной владельцем
> подаче, 01.09 снята со страницы («убери пока этот блок») и удалена при
> чистке репозитория.** Живая страница заканчивается FAQ. Слайд 7
> «Авторитет» так и обойдён — контента нет.
>
> **Как вернуть.** Файлы поднимаются из коммита-снимка `0f33445`:
> `src/components/QuoteSection.tsx`, `src/data/quote.ts`, макетная
> `quote.html` + `src/quote.tsx` + `src/dev/QuoteLayouts.tsx`, кадр
> `src/assets/coach-band.webp`. Дальше — вписать `<QuoteSection />` после
> `<FaqSection />` в `App.tsx` и сделать три вещи, которые при снятии
> откачены обратно:
> — передвинуть сход в `bg-pitch` из FAQ в цитату (он всегда у последней
>   секции страницы, иначе гасит фон на стыке двух);
> — вернуть в `CourseBackdrop` пятно 13 (центр `prog+5250`, синее, плотность
>   .16) — без него секция ложится на голый тон, как ложились слайды 6 и 8;
> — поднять слой `CourseBackdrop` с 7600 до 7800px.
>
> **Что это была за подача.** Журнальная врезка с обтеканием: снимок врезан в
> текст, текст его обтекает; материя взята у FAQ — волосяная линия сверху,
> синяя нить слева, имя Bebas-капслоком 17px, у врезки ни рамки, ни
> скругления, края растворяются масками. Выбрана владельцем из девяти подач в
> трёх заходах. Высота секции 319 / 329 на 430 и 360.
>
> **Текст и кадр.** В ТЗ по слайду нет ничего, кроме строки «мотивационная
> цитата и жизнеутверждающее фото» (Q20). 01.09 владелец передал и текст, и
> кадр сам: текст — блок с работающего `gadjiev.pro`, кадр —
> `coach-band.webp`. Оба лежат в снимке `0f33445`.
>
> **Открытый вопрос:** Q20 закрыт наполовину — для слайда 9 контент есть, для
> слайда 7 по-прежнему нет ничего.

**Правило: не переходить к следующему слайду, пока владелец не скажет
«Переходи».** Даже если текущий выглядит законченным.

---

## Железные условия проекта

Нарушение любого = переделка.

1. **Только мобильная вёрстка.** Десктопа не будет вообще, это обязательное условие
   владельца. Проектная ширина 430px, проверять на 360px. Страница по горизонтали не
   скроллится никогда.
2. **Бэкенда не делаем.** Только макет. Кнопки оплаты — заглушки с `TODO`. В `api/`,
   `db/`, в плагин API внутри `vite.config.ts` не лезем. `DATABASE_URL` не нужен,
   `npm run db:init` не запускать.
3. **Тексты не сочиняем.** Всё берётся дословно из `01-SPEC.md`. Прошлый черновик
   программы был написан «по логике курса» и целиком выброшен — второй раз так не
   делаем.
4. **Ассеты не подменяем.** Если раскладка не сходится — меняем контейнер и CSS, а не
   картинку.
5. **Проверка перед сдачей:** `npm run typecheck`. Тестов в проекте нет, это
   единственная проверка, и она обязана быть зелёной.

---

## Принятые решения

| Дата | Решение |
|---|---|
| 30.08.2026 | Десктопа не будет. Всё помещаем в мобильную ширину (Q22 закрыт) |
| 30.08.2026 | Бэкенд отложен до готовности макета |
| 30.08.2026 | Открытые вопросы решаем по ходу, на том слайде, где возникают — не списком заранее |
| 30.08.2026 | Работаем по одному слайду за сессию, новый слайд — новый диалог |
| 30.08.2026 | Красный (`--color-blood`) в дизайне не используем и не обсуждаем — акцент страницы синий (`#6AA0FF`) |
| 30.08.2026 | ~~Раскладки не примеряем на отдельной dev-странице~~ — отменено 30.08: для «Для кого?» владелец сам попросил страницу вариантов (`audience.html`) после трёх отказов подряд |
| 30.08.2026 | ~~Сегменты — лента варианта 02~~ — заменено: **вариант 09 «постер-свайп»**. Карточка 268×370, кадр во всю площадь, текст поверх затемнения. Лента и snap остались от 02 |
| 30.08.2026 | **Обложка и лента слиты в одну секцию (раскладка B).** Снимок стадиона — фон всего блока, карточки заходят на него. AboutSection со страницы снята |
| 30.08.2026 | **Кнопка «К программе» на слайде 2 — вариант 03 «разметка поля»** (`.btn-hero-turf`): газон с полосами и линией штрафной. Вариант 02 (фото в заливке) примерялся и снят — на этой секции фоном лежит тот же кадр |
| 30.08.2026 | **Фотографии сегментов заказчика устраивают** — претензии были к подаче блока, не к кадрам. Менять контейнер и CSS, не снимки |
| 30.08.2026 | ~~AboutSection и AudienceSection не удаляем~~ — отменено: AboutSection снята со страницы, слайд 2 держит одна AudienceSection. Файл остался в репозитории |
| 30.08.2026 | **Слайд 3: раскладка «фото сверху, текст под ним» (Q9 закрыт).** «Справа фото, слева текст» из ТЗ — десктопная геометрия, на 430px шесть регалий рядом с кадром дают строки по три слова. Порядок сверху вниз повторяет порядок ТЗ |
| 30.08.2026 | **Портрет для слайда 3 собран из `bg.png` (Q10 закрыт нашей стороной).** Ждём подтверждения владельца или съёмочного кадра |
| 30.08.2026 | **Вводный абзац слайда 3 снят (Q8).** «Более 50 лет в большом футболе» в ТЗ нет, а тексты берём дословно. Обратимо одной строкой |
| 30.08.2026 | **Золотые 3D-бейджи регалий сняты.** Поле `icon` убрано из `achievements.ts`, файлы `1–6.webp` остались в assets. Рядом с фотографической подачей слайдов 1 и 2 читались игровыми ачивками |
| 31.08.2026 | ~~Геометрия ТЗ «фото справа, текст слева» упирается в кадр~~ — **неверно, отменено в тот же день.** Вывод был сделан из чтения «слева весь текст слайда». В ТЗ следом идёт «далее — списком регалии»: слева стоят только надзаголовок и имя, регалии идут под ними. В таком чтении раскладка помещается спокойно |
| 31.08.2026 | **Слайд 3 — раскладка ТЗ колонками (вариант 07).** Слева надзаголовок и имя, справа портрет в вылет к правому краю (44% ширины, 280px), регалии списком под строкой на всю ширину. Q9 («фото сверху, текст под ним») тем самым закрыт в пользу дословной геометрии ТЗ |
| 31.08.2026 | **Под узкую колонку сделан отдельный кадр** `coach-portrait-side.webp`. Общий портрет в колонке 180px давал голову 70px — «расположен прямо в кадре» не читалось |
| 31.08.2026 | **Регалия — самый громкий текст слайда 3 после имени** (Bebas капслок 21px). Была 14.5px серым: единственное содержание слайда было тише всего на экране |
| 31.08.2026 | **Точка на вертикальной черте у регалий убрана.** Приём обещал хронологию, которой в этих шести пунктах нет: клубы РПЛ, две премии и учёная степень в последовательность не выстраиваются. Разделители — волосяные линии |
| 31.08.2026 | **Слайд 3 — вариант 04 «одной фотополосой», на съёмочном кадре заказчика.** Раскладка колонками (07) отклонена владельцем. Текст и фигура лежат в одном снимке: слева тёмные трибуны под надзаголовок и имя, справа ГМ — геометрия ТЗ без границы между колонками |
| 31.08.2026 | **Края кадра растворяются, а не обрезаются** (`.coach-scene-sharp` / `.coach-scene-blur`). Требование владельца: фон страницы должен идти сквозь секцию бесшовно, тем же приёмом, что стадион на слайде 2 |
| 31.08.2026 | **Запас над макушкой вшит в сам файл** — канва наращена вверх на 250px отражённой, размытой и притемнённой полосой, поверх положена заливка тоном страницы. Без неё маска проявляла на стыке зарево прожекторов светлой линией |
| 31.08.2026 | ~~Собственный тон «Для кого» гаснет к низу секции~~ — **пробовали и откатили в тот же день.** Тональная база `CourseBackdrop` стартует ровно этим же `#0c141d` на границе секций, то есть цвета и так стыкуются цвет в цвет. Погасив тон, открыли под ним `bg-pitch` и получили скачок 7 → 19 — тот самый стык, который владелец увидел сразу |
| 31.08.2026 | **Верх кадра залит `#0c141d`, а не `#05080b`.** Заливка должна совпадать с фоном на границе секций, иначе снимок проявляется темнее окружения и растворение читается полосой. Замер: спад 19 → 7 за 10px против плавного за 60px |
| 31.08.2026 | **Кадр приближен в 1.29x** (окно 150..1450 исходника). «Гаджи сильно отдалён» — приблизить можно только сузив кадр по горизонтали, и запас взят справа: за фигурой лежало 264px пустого газона с воротами. Тёмная левая половина под текст при этом сохранена — фигура начинается ровно на 50% ширины |
| 31.08.2026 | **На 360px имя сбавлено до 30px** (`max-[380px]:text-[30px]`). При 32px «МУСЛИМОВИЧ» — 153px в колонке 152px, слово упиралось в фигуру |
| 31.08.2026 | **Текст слайда 3 прижат к верху кадра, а не к его низу.** Прижатым к низу надзаголовок начинался на 137px от верха секции против 14 и 26 у соседних — слайд читался опущенным. Отступ пиксельный, не процентный: кадр масштабируется с шириной экрана, а высота имени — нет |
| 31.08.2026 | **Кадр укорочен до 727-й строки исходника** (860×600, 300px на 430). Текст ушёл наверх, и под именем оставалось 240px пустого газона слева — дыра посреди слайда |
| 31.08.2026 | **Иконки регалий вернулись — предметными фотографиями, не бейджами.** Лист `docs/icon.png` от заказчика нарезан на шесть плиток 176×176 с прозрачными углами. Прежние золотые 3D-бейджи (`1–6.webp`) остаются снятыми: претензия к ним была в том, что они читались игровыми ачивками, а фотографии из того же материала, что вся страница |
| 31.08.2026 | ~~Текст слайда 3 прижат к верху кадра~~ — **отменено, выбрана раскладка «имя по центру кадра».** Владелец: «не выровнял вообще». Текст сверху при кадре в полный рост оставлял под именем пустую тёмную половину: имя в левой половине, фигура в правой, вместе — диагональ. Теперь и текстовый блок, и лицо центрированы по полосе |
| 31.08.2026 | **Центр лица выведен в центр полосы.** Отсюда запас неба 331px: PAD + 184 = (PAD + H) / 2 при H = 700. Надзаголовок при этом начинается ниже, чем на слайдах 1 и 2 — владелец это видел в описании варианта и выбрал его |
| 31.08.2026 | ~~Кадр укорочен до полосы 215px~~ — **отменено в тот же день.** «Ассет сжался, блок какой-то маленький». Полосу задирать под высоту текста нельзя, снимок должен оставаться крупным |
| 31.08.2026 | ~~Слайд 3 — кадр-вставка (полоса или колонка) плюс текст рядом~~ — **отменено целиком.** Четыре попытки выровнять фотографию и текст друг относительно друга владелец забраковал. Причина была структурной: пока снимок — вставка внутри секции, его всегда надо к чему-то приставлять |
| 31.08.2026 | **Слайд 3 — подача «Досье на поле» (вариант 01 новой макетной).** Кадр в полный рост лежит подложкой всей секции, текст — в левой тёмной колонке снимка. Выравнивать нечего: колонка задана самой фотографией |
| 31.08.2026 | **Боевой кадр — `docs/newbg.png` в полный рост.** Прислан заказчиком специально «чтобы наложить регалии слева от ГМ». Широкий `3sl.png` остался на макетных вариантах |
| 31.08.2026 | **Снимок-подложка масштабируется по ширине экрана, не по высоте секции.** `object-cover` увеличивал кадр на узких экранах, и фигура наезжала на текст: левый край фигуры 48% на 430 против 27% на 360 |
| 31.08.2026 | **Макетная `/coach.html` переписана целиком.** Семь прежних подач сняты, восемь новых: досье на поле, нижняя шторка, плитки поверх кадра, лента со snap, профиль приложения, рельса с узлами, карточка-обложка, витрина наград |
| 31.08.2026 | ~~Верхний стык слайда 3 сделан внутри файла кадра~~ — **откачено владельцем в тот же день:** лицо посветлело со 68 до 130, но фигура опустилась на 220px канвы и разметка съехала. Приглушённое лицо принято как плата за стык |
| 31.08.2026 | **Анимация появления секций при скролле снята со страницы целиком.** Блоки больше не всплывают. Удалены атрибуты `data-reveal`, класс `.reveal-child` и CSS-правила под них; хук `useReveal` остался в репозитории неиспользуемым |
| 31.08.2026 | **Кадр слайда 3 растягивается по высоте секции, якорь 78%.** Владелец: «шестая регалия сильно ниже, чем ГМ». Теперь фигура во весь рост блока, регалии идут вдоль неё. Плата: нижние строки частично на брюках, шторка гасит их сильнее книзу |
| 31.08.2026 | **Лицо осветлено локально в файле кадра плюс шторка отпущена раньше.** Условие владельца: не менять вёрстку и не двигать фон. Яркость лица на странице 74.5 → 106 |
| 31.08.2026 | **Слайд 4 остаётся аккордеоном.** Заказчик одобрил его в ТЗ дословно, поэтому раскладку не переизобретали: поменялись содержимое, состав плашки и состояния |
| 31.08.2026 | **Черновик программы выброшен из боевых данных.** Шесть выдуманных модулей с хронометражом переехали в `src/dev/programDraft.ts` — только чтобы макетная `/program.html` собиралась. В `src/data/program.ts` теперь дословные тексты ТЗ |
| 31.08.2026 | **Результат («Вы сможете …») вынесен наружу панели раскрытия.** По ТЗ это статичная часть блока: закрытая карточка обязана отвечать на вопрос «что я получу», а не только называть модуль |
| 31.08.2026 | **«УРОКОВ: N» — в шапке справа от названия (Q11 закрыт).** В ТЗ строка идёт последней в блоке, но в закрытом виде счётчик помогает решить, разворачивать ли список. У неутверждённых модулей на его месте прочерк, чтобы колонка 34px не схлопывалась |
| 31.08.2026 | **Модуль без утверждённых уроков не раскрывается (Q13 закрыт).** Шапка у него не `<button>`, а обычный заголовок — ни фокуса, ни `aria-expanded`; вместо списка строка «Программа модуля утверждается» |
| 31.08.2026 | **Картинок у модулей нет (Q12 закрыт).** В ТЗ формат блока описан без них. Шесть рендеров `course-*.webp` остаются в assets под слайд 5, где картинка требуется прямым текстом |
| 31.08.2026 | **Описание-подзаголовок под названием модуля снято.** Оно было из черновика, а названия по ТЗ длинные: на 430px две строки названия плюс две строки описания давали плашку под сотню пикселей. Плашка ужата с 78 до 64px по минимуму |
| 31.08.2026 | **Значок «play» у урока заменён порядковым номером.** Хронометража и форматов урока в данных больше нет — плеер обещал бы то, чего нет |
| 31.08.2026 | **Итоговая подпись «6 модулей, 35 уроков» снята.** Настоящих уроков сейчас 12 из неизвестного числа, любая цифра под секцией была бы враньём. Вернётся с остальными модулями |
| 31.08.2026 | **Бонусный блок помечен тёплым (`#FFC14A`): рамка, отсвет и слово «БОНУС» вместо номера.** В ТЗ он «БОНУС», а не «модуль 6», и в тарифной таблице входит только в ПРЕМИУМ и ВИП |
| 31.08.2026 | **Кнопка «Посмотреть тарифы» — та же зелёная подача, что «К программе»** (`.btn-hero-turf`). Обе переносят с этажа на этаж, одинаковыми они читаются одной системой навигации. Якоря `#tariffs` пока нет — слайд 6 не сделан, в коде стоит `TODO` |
| 31.08.2026 | ~~Слайд 5 — лента из четырёх экранов по разбивке ТЗ~~ — **отвергнуто владельцем в тот же день.** Свайп прятал 8 пунктов из 11 в самой продающей секции, а одиннадцать одинаковых плиток давали одиннадцати разным вещам один вес. Разбивка ТЗ (3+2+3+3) сама по себе не смысловая: она делит пункты по месту в списке, а не по тому, на какой вопрос они отвечают |
| 31.08.2026 | **Одиннадцать пунктов слайда 5 отвечают на три разных вопроса, а не на один.** Как устроены занятия / что человек делает внутри потока / что остаётся после. Это деление положено в основу всех четырёх подач на `/learning.html`. Ядро — обратная связь, микрогруппы и разборы с Гаджи Муслимовичем: именно они доказывают, что курс не папка с видео |
| 31.08.2026 | **В слайде 5 ничего не прячем — ни свайпа, ни раскрытия.** Это состав того, за что платят 50–100 тысяч, он должен читаться прокруткой. Плата — высота секции около 1000–1100px, и это принято сознательно |
| 31.08.2026 | **Данные слайда 5 лежат плоским списком с ключами** (`LEARNING_ITEMS` + `pickLearning`), а группировка — снаружи. У разных подач она разная, а тексты одни и те же. Разбивка самого ТЗ осталась отдельным экспортом `LEARNING_TZ_SCREENS` |
| 31.08.2026 | **Контурные иконки как временный визуальный ряд (Q14 открыт).** Десять новых значков в общей системе набора (viewBox 24, обводка 1.6) плюс готовый `SoccerFieldIcon` под «Практику». Рендеры `course-*.webp` не взяты: они на белом фоне, их шесть на одиннадцать пунктов, и с фотографическими значками регалий в один набор не складываются |
| 31.08.2026 | **Q14 подешевел: при делении на этажи иллюстраций нужно 3, а не 11.** По одной на этаж. Три кадра заказчик даст, одиннадцать будет собирать месяц |
| 31.08.2026 | **Свет секции нельзя держать внутри самой секции — он обрезается её краем.** Слой с радиальными пятнами внутри «Как проходит обучение» давал ступеньку на стыке с «Программой»: замер по пикселям на середине ширины — rgb(10,18,25) над границей против rgb(13,23,34) под ней, скачок яркости 16.4 → 21.3 в два пикселя. Свет перенесён в общий `CourseBackdrop`, который границ не знает. После переноса скачок ±0.8 на 430 и ±1.3 на 360, то есть наклон градиента, а не граница |
| 31.08.2026 | **Фон «переливается» цветами категорий, а не абстрактно.** Пять цветных пятен ниже программы идут в том же порядке, что категории слайда 5: бирюза на низ программы, синий под платформой и ступенями, лиловый в длинном процессном блоке, мята на бесплатных доступах, золото на итоге. Центры чередуются по горизонтали, поэтому при прокрутке оттенок переходит по диагонали, а не гаснет на месте |
| 31.08.2026 | **Альфа цветных пятен подобрана под контраст, а не на глаз.** Пик мяты даёт фон rgb(19,52,51), пик лилового rgb(28,32,61), пик золота rgb(44,42,32) — белый текст в 55% читается на всех трёх с 5.2–5.7:1 при норме 4.5. Золото ослаблено с .17 до .14: на .17 тёплое пятно читалось коричневым |
| 31.08.2026 | **Тональная база страницы больше не уходит в чёрный.** Последним стопом стоял `#05080b` на `prog+950` — тон умирал на середине «Программы», и вся нижняя половина страницы лежала на чистом чёрном. Владелец: «фон стремится к чёрному, а хотелось бы, чтобы он продолжался с блока „Программа обучения“». Теперь база спускается до `#0a1119` (токен `--color-panel`) и держится до низа слоя |
| 31.08.2026 | **Сход в `bg-pitch` переехал из «Программы» в «Как проходит обучение».** У программы он гасил фон ровно на стыке двух секций — это и было видно. Теперь гаснет только низ последней секции, перед подвалом |
| 31.08.2026 | **Высота слоя `CourseBackdrop` 4200 → 5600px.** Пока база уходила в чёрный, нехватка высоты была не видна: под слоем лежал тот же `#05080b`. Теперь база держит тон до низа, и слой обязан покрывать всю обёртку — замер: 4543px на 430, 5033px на 360, плюс до 400px на раскрытый модуль |
| 31.08.2026 | **Свет слайда 5 живёт внутри самой секции, а не в общем фоне.** Пятна в `CourseBackdrop` привязаны к верху программы, а программа — аккордеон: при раскрытии модуля всё, что ниже, уезжает от своего света. Внутри секции пятна заданы в процентах её высоты и едут вместе с ней |
| 31.08.2026 | **Слайд 5 — подача «Кабинет» (вариант 01 с `/learning.html`), выбор владельца.** Платформа героем, три категории цветом, у каждой своя форма: ступени с номерами / карты с рамкой / строки со значком |
| 31.08.2026 | **Категории и их цвета живут в `data/learning.ts`** (`CATEGORIES`), а не в компоненте: макетная и боевая секция обязаны показывать одно и то же. Там же плоский `LEARNING_ITEMS` с ключами и `pickLearning` |
| 31.08.2026 | **Платформа — герой слайда 5, а не строка списка** (прямое требование владельца). Показывает саму себя: окно кабинета с тремя настоящими модулями из `data/program.ts`, полоса прохождения, «24/7» из ТЗ крупно. Ничего не выдумано |
| 31.08.2026 | **Цвет кодирует категорию:** синий — как проходит обучение, мята — что получаете бесплатно, золото — что остаётся после. Мята холодная и не спорит с зеленью кнопок `.btn-hero-turf`, поэтому карта доступа не выглядит кнопкой; золото уже работает на странице меткой «сверх основного» (бонусный модуль программы) |
| 31.08.2026 | ~~Газон и разметка поля в слайде 5~~ — **сняты по прямому указанию владельца** («не надо туда пихать футбольное поле») |
| 31.08.2026 | **Цвета мало — у категории должна быть своя форма.** Ступени с номерами / карты с рамкой / строки со значком. Пока все три категории были одинаковыми строками, цвет оставался единственным различием и секция читалась стеной |
| 31.08.2026 | **Тело пункта — 12px white/55 против названия Bebas 23px белым.** При 12.5px/58 название и текст читались одним серым абзацем. 11.5px пробовали и вернули: собственное правило набора — не мельче 12px |
| 31.08.2026 | **Секцию нельзя рисовать дашбордом — у страницы записана своя доктрина.** В `index.css` у `.btn-hero`: «радиус 6px вместо мягких 14 — язык тактических схем, а не дашборда». Слайд 5 в первых двух заходах нарушал её: скруглённые плашки 14–16px, мягкая синяя подсветка, значки в квадратиках с 10%-заливкой. Радиус в новом наборе 6px или ноль |
| 31.08.2026 | **Названия пунктов слайда 5 — Bebas 21–29px, а не 16–20.** Страница говорит заголовками в 32px; секция, набранная в 16–20, шепчет рядом с соседями. Названия должны работать заголовками, а не подписями |
| 31.08.2026 | **Свет на странице делается волосяной линией и кромкой в 1px, а не радиальным пятном.** Синий отсвет под подложкой, рамкой, значком и текстом сводит их в один тональный диапазон, и контрастных ступеней не остаётся |
| 31.08.2026 | **Газон рисуется CSS, а не картинкой** (полосы под 115°, шаг 36px — приём `.btn-hero-turf`). В подаче «Тактическая доска» ядро стоит на тёмном газоне: яркая зелень занята кнопками, повторять её нельзя — блок начал бы выглядеть кликабельным |
| 31.08.2026 | **Сход в `bg-pitch` переехал из «Как проходит обучение» в «Выбери свой тариф».** Он должен рисоваться по низу последней секции страницы: у предпоследней он гасит фон ровно на стыке двух и читается ступенькой. Тем же приёмом слой в своё время уехал из «Программы» в «Как проходит обучение» |
| 31.08.2026 | **Высота слоя `CourseBackdrop` 5600 → 7200px.** Слайд 6 добавил обёртке 1335px (5878 на 430, 6382 на 360), и слой кончался раньше неё — на его нижней границе появлялась ступенька. Правило: добавили секцию — проверили запас слоя |
| 31.08.2026 | **Цветное поле продлено под тарифы: синее пятно под лентой, золотое под блоком обращений.** Раньше цвет кончался на `prog+3160`, а секция начинается на `prog+3319` — вся она лежала на голом тоне, и «переливание» обрывалось ровно там, где страница просит денег |
| 31.08.2026 | **Слайд 6 — подача 01 «Столбцы», выбор владельца.** Тарифы остаются столбцами из ТЗ, столбцы листаются: карточка 262px прилипает по центру, лента открыта на ПРЕМИУМЕ. Подачи 02 «Лестница» и 03 «Матрица» отклонены |
| 31.08.2026 | **У каждого тарифа свой цвет — материал ступени: сталь → синий → золото.** Требование владельца «придумай аналогию цвета к названию». Ни один цвет не выдуман под слайд: сталь — отсутствие цвета у точки отсчёта, синий — акцент страницы у тарифа, который надо продавать, золото на странице уже означает «сверх основного» (бонусный модуль, «что остаётся после»). Красного в шкале нет — решение 30.08 |
| 31.08.2026 | **Цвета мало: у каждого тарифа свой приём выделения.** СТАНДАРТ — без верхней грани и с самой тихой рамкой; ПРЕМИУМ — шапка во всю ширину со звездой и «ХИТ»; ВИП — золотая грань в 2px по верху. Та же доктрина, что в слайде 5 |
| 31.08.2026 | **Возрастание тарифов показано делениями, а не словами** (три штриха, залито по номеру ступени). Подписи «базовый / расширенный» пришлось бы сочинять, а тексты берём только из ТЗ |
| 31.08.2026 | **Неактивный столбец — `opacity .52` плюс `saturate(.6)`.** Требование владельца. Без saturate стальной СТАНДАРТ приглушённым не выглядел: он и в полную силу такой же серый |
| 31.08.2026 | **Плоская синяя кнопка «Оплатить полностью» осталась только у ПРЕМИУМА**, у СТАНДАРТА и ВИП она контурная в их цвете. Иначе самый громкий элемент карточки одинаковый у всех трёх, и «выделить хит больше всех» ломается именно на кнопке. Вернуть всем синюю — убрать одно свойство |
| 31.08.2026 | **У ПРЕМИУМА тень под карточкой, а не свечение по краю.** Свечение занято героем слайда 5 (платформой). Ленте добавлен `py-3`: `overflow-x: auto` делает вертикальный overflow неявно скрытым, и тень срезалась по нижней грани |
| 31.08.2026 | **Слайд 6: пятнадцать кнопок сведены к пяти (Q17 закрыт).** Три обращения в WhatsApp (рассрочка, клубы и академии, отдельные модули) одинаковы для всех тарифов и от выбора не зависят — вынесены одним блоком под сетку. В карточке остаются «Оплатить полностью» и «Забронировать за 10 000 ₽», в иерархии заливки ячеек исходной таблицы |
| 31.08.2026 | **Модули 1–5 в тарифах свёрнуты в одну строку.** Они входят во все три тарифа: пять строк подряд с одинаковыми галочками ничего не сравнивают, а забирают треть высоты. Свёртка живёт в данных (`MATRIX_FOLDED`), полный список остаётся в `FEATURES` |
| 31.08.2026 | **Названия позиций тарифов приведены к одному виду (Q23 нашей стороной).** «ГРУППОВЫЕ РАЗБОРЫ ТРЕНИРОВОК ОТ ГАДЖИ ГАДЖИЕВА МУСЛИМОВИЧА» → «Разборы тренировок»; «ЖИВАЯ-ВСТРЕЧА» без дефиса; имя везде «Гаджиевым Гаджи Муслимовичем», как в слайдах 1 и 3. Подтверждения ждём |
| 31.08.2026 | **Описаний позиций в тарифах нет.** Девять из восемнадцати дословно описаны этажом выше, в «Как проходит обучение»; повторять — вторая стена текста сразу после первой. В сетке заказчика описаний тоже нет, только названия |
| 31.08.2026 | **ПРЕМИУМ выделен рамкой, заливкой и шапкой со звездой, но не свечением.** Свечение по краю на странице занято героем слайда 5 (платформой) — второе такое же перестало бы работать выделением. Цвет выделения — синий акцент страницы `#6AA0FF` |
| 31.08.2026 | **Цена в шапке матрицы пишется полностью (49 900), а не округляется до «50к».** Округление превращало 49 900 в 50 000, то есть в неверную цену. Знак рубля снят — в колонку 56px запись с ним не встаёт, полная стоит под таблицей у кнопки |
| 31.08.2026 | **Лента тарифов прилипает по центру, а не по левому краю.** Со `snap-start` лента, открытая на ПРЕМИУМЕ, показывала его вплотную к левому краю, и СТАНДАРТ уходил за экран целиком — без намёка, что слева что-то есть. Боковое поле `calc(50% - 131px)` работает и на 430, и на 360 без медиазапроса |
| 31.08.2026 | **Кнопки в конце слайда 5 нет.** В ТЗ для этого слайда она не описана, а «Посмотреть тарифы» уже стоит этажом выше, в конце программы. Две одинаковые кнопки подряд читались бы сбоем, а не навигацией |
| 02.09.2026 | **Модалка оплаты — нижний лист на нативном `<dialog>`, а не свой оверлей.** `<main>` обёрнут в `overflow-hidden` (без него якорная прокрутка уносит секции), и собственный слой пришлось бы портировать в `document.body` либо ловить обрезку. Модальный `<dialog>` уходит в top layer и даёт даром захват фокуса, Esc и `inert` для страницы под собой |
| 02.09.2026 | **Два листа оплаты различаются по действию, а не по тарифу.** Полная оплата идёт в цвете ступени (сталь → синий → золото), бронь — газонный зелёный `#21B365` один на все три. Раскраска по тарифу дала бы у СТАНДАРТА два одинаковых стальных листа, а владелец просил, чтобы модалки отличались цветом. Зелёный на странице уже означает «шаг дальше» (`.btn-hero-turf`), и бронь — именно шаг: место придержано, оплата потом. Ни одному тарифу он не принадлежит |
| 02.09.2026 | **Цвет тарифа из листа брони не пропадает** — им набраны название и рамка панели. Тариф виден всегда, действие окрашено отдельно |
| 02.09.2026 | **Условия в листе — `faqAnswer()` из `data/faq.ts`, а не второй литерал.** Лист повторяет слова FAQ дословно (бронь — `booking`, оплата — `payment`). Разъедься эти два места, страница начнёт противоречить сама себе. Остаток брони считается из `RESERVE_AMOUNT`, а не набирается |
| 02.09.2026 | **Заголовки листов — «Оплата курса» и «Бронь места», подпись кнопки в них не повторяется.** Дублировать «Забронировать за 10 000 ₽» в шапке и на кнопке значило дважды сказать одно; оба оборота есть в ТЗ («бронь места за 10 000 рублей», «способы оплаты») |
| 02.09.2026 | **Заглушка Q19 переехала из карточки в лист.** Раньше кнопка тарифа сама меняла подпись на «Скоро»; теперь она открывает лист, а «Скоро» отвечает главная кнопка листа. Человеку сначала показывают, за что и сколько, и только потом он упирается в отсутствующего провайдера |
| 02.09.2026 | **Лист покупки говорит языком интерфейса, а не витрины.** Две версии владелец отверг: первая была собрана из пяти одинаковых коробок с рамкой 1px, вторая заговорила капслоком Bebas в подписях полей, трекингом .14em и радиусом 6px. Обе читались тактической схемой. Просьба была дословно: «нормальная стандартная форма с красивой подачей». Отсюда: подписи обычным текстом 13–15px без капслока; радиус 10px у полей и кнопки, 16px по верхним углам листа; одна панель на группу вместо рамки на каждом элементе; один акцент на весь лист. Капслок Bebas остался ровно в двух местах, где работает, — заголовок листа и сумма к оплате |
| 02.09.2026 | **Кнопка листа — не `.btn-hero`.** Та набрана Bebas-капслоком в 20px с трекингом, это голос витрины. В форме оплаты кнопка называет действие и сумму обычными словами: «Оплатить 64 900 ₽». Сумма на кнопке — стандарт чекаута: человек в последний раз видит, сколько с него спишут, не поднимая глаз к сводке |
| 02.09.2026 | **Полей карты в листе не будет.** У Prodamus порядок такой: сайт показывает заказ и собирает контакты, карта вводится уже на защищённой странице сервиса. Наш лист — шаг перед оплатой, а не оплата |
| 02.09.2026 | **Способ оплаты — одна строка-панель, а не плашки-переключатели.** Плашками это было в отвергнутой версии. Выбрать рассрочку на странице нельзя, всё решается на стороне сервиса, а элемент, похожий на переключатель, обещает обратное. Список способов склеивается в строку через запятую, поэтому в данных он лежит строчными буквами — «Банковская» посреди фразы читалась бы опечаткой |
| 02.09.2026 | **Описания преимуществ стоят целиком, без обрезки по строкам.** `line-clamp-2` вешал «…» посреди фразы, и список читался недоделкой. Лист и так прокручивается |
| 02.09.2026 | **Кольцо фокуса листа исключает поля и чекбоксы через `:not()`, а не отдельным правилом.** У `.pay-input:focus-visible` та же специфичность (0,2,0), что у `.pay-sheet :focus-visible`, а общее правило стоит в файле ниже и потому выигрывало: поверх цветной рамки поля рисовался второй, белый контур |
| 02.09.2026 | **Ровно три преимущества на тариф, список задан руками (`HIGHLIGHTS`).** Правка владельца поверх предыдущего решения. Взять «первые три» из прибавки нельзя: порядок в таблице — порядок колонки ВИП, а не порядок силы, и у ПРЕМИУМА в витрину попадала бы «Обратная связь при проверке домашних заданий» вместо разборов с Гаджиевым. Выбрано: СТАНДАРТ — модули, сертификат, портфолио; ПРЕМИУМ — практика, NANOFOOTBALL, разборы; ВИП — SCOUTWAY, NANOFOOTBALL, консультации. Микрогруппы и живую встречу в витрину не берём: у обеих флаг `pending` (Q23), а ставить туда позицию, которую могут убрать, нельзя |
| 02.09.2026 | **У ВИПа в витрине обе платформы парой — SCOUTWAY и NANOFOOTBALL** (правка владельца). Формально NANOFOOTBALL прибавкой ступени не является: он приходит к ВИПу из ПРЕМИУМА. Но `HIGHLIGHTS` — это «главное в тарифе», а не «чем он отличается от предыдущего», и ВИП единственный, у кого обе платформы разом. Полный состав со всеми восемнадцатью позициями и так лежит в карточке под лентой |
| 02.09.2026 | **Состав способов оплаты задан владельцем: СБП, оплата любой картой РФ, банковская рассрочка, исламская рассрочка.** Прежний список, разобранный из ответа FAQ, снят. ⚠ **Расхождение, требующее решения заказчика:** ответ FAQ на слайде 8 по-прежнему обещает доверительную рассрочку и СПЛИТ, а список их не содержит — пока оба текста на одной странице, она спорит сама с собой. Править надо один из двух |
| 02.09.2026 | **У исламской рассрочки справа кнопка «Менеджер» в фирменном зелёном WhatsApp** (`#25D366`, глиф мессенджера, подпись `#08291A` — 7.9:1 против нечитаемых 2.1:1 у белой). Она единственная из четырёх не оформляется нажатием. Цвет не спорит с газонным `#21B365` главной кнопки на листе брони именно из-за глифа: с ним кнопка читается как «написать в WhatsApp», а не как второе главное действие. Тот же довод записан у `ContactBlock`. Q18: номера нет — кнопка рисуется боевой, но никуда не ведёт |
| 02.09.2026 | **Под каждым способом стоит короткая строка описания.** ⚠ Эти четыре строки **не из ТЗ** — заказчик описаний способов не давал. Всё сказанное в них про сами инструменты, а не про курс. У исламской рассрочки описание сокращено до «Оформляется индивидуально»: «через менеджера» рядом с кнопкой «Менеджер» было тавтологией, а на 360 ломало строку на четыре |
| 02.09.2026 | **Сводка ошибок над формой снята** (правка владельца: «у нас сами поля ввода показывают, если есть ошибка»). Одну её работу пришлось оставить в другом виде: кнопка стоит в подвале листа, а проваленное поле может быть выше границы прокрутки, и без сводки человек нажимал «Оплатить» и не видел, что изменилось. Теперь при неудачной отправке лист сам прокручивает первое проваленное поле в центр и ставит в него курсор (`scrollIntoView` + `focus({ preventScroll: true })`, мгновенно под `prefers-reduced-motion`) |
| 02.09.2026 | **У обязательного согласия появился свой текст ошибки** — «Без согласия на обработку данных оплата невозможна». До этого о непоставленной галочке говорила только красная рамка квадрата в 1px на 21px: самый тихий сигнал в форме, и после снятия сводки он остался бы единственным |
| 02.09.2026 | **Чекбокс нельзя прятать утилитой `sr-only` — это стоило бага «форму скомкивает».** `sr-only` даёт `position: absolute` без смещений, и содержащим блоком становится ближайший позиционированный предок: `.pay-sheet`, а не прокручиваемая `.pay-scroll`. Инпут переставал ездить с содержимым и застревал на исходной координате — замер `top: 1434px` при высоте листа 820. При нажатии браузер уводил на него фокус и тянул в видимую зону, прокручивая **каждого** прокручиваемого предка; единственным подходящим оказывался сам `<dialog>`, и лист подпрыгивал на 554px. Лечение: инпут стал прозрачным (`opacity: 0`) и лёг ровно поверх нарисованного квадрата, а подписи добавлен `position: relative`. **Правило:** абсолютный элемент внутри прокручиваемой области обязан иметь содержащий блок внутри неё же — в проекте эта грабля уже была дважды, с `CourseBackdrop` |
| 02.09.2026 | **Полоса прокрутки в листе скрыта** (`scrollbar-width: none` плюс `::-webkit-scrollbar`), тем же приёмом, что у ленты тарифов и ленты сегментов слайда 2. Прокручиваемость осталась, убран только индикатор: он садился поверх правого края панелей и читался их частью. Что лист листается, видно по обрезанному содержимому у нижней кромки |
| 02.09.2026 | **Квадратные плашки под значками способов сняты, значки стоят прямо на подложке строки.** Четыре одинаковых квадрата в столбик читались вторым списком поверх первого, а цветной знак СБП в плашке выглядел наклейкой на кнопке |
| 02.09.2026 | **Три значка способов взяты из набора Lucide** (lucide.dev, ISC) и вставлены путями, а не пакетом: нужны ровно три штуки. Карта — `credit-card`, банковская рассрочка — `landmark` (здание банка: календарь говорил бы «платёж во времени», что верно для обеих рассрочек сразу, а различает их слово «банковская»), исламская — `handshake`. Обводка оставлена авторская, 2 вместо проектных 1.6: на 20px более тонкая линия бледнеет рядом с цветным знаком СБП в том же столбце |
| 02.09.2026 | **Прежняя строка-пояснение под исламской рассрочкой** («Оформляется через менеджера — напишите нам, чтобы согласовать условия»). Она единственная из четырёх не оформляется нажатием, и сказать об этом надо там, где человек её увидел, а не мелким шрифтом внизу. Строка становится ссылкой в WhatsApp, когда появится номер (Q18) |
| 02.09.2026 | **У СБП стоит настоящий фирменный знак, у остальных трёх — рисованные глифы.** Знак лежит отдельным файлом `src/assets/sbp.svg`, а не компонентом среди значков проекта, ровно потому, что это чужой товарный знак: заменить его на файл из брендбука НСПК — это положить другой файл, не трогая код. Из логотипа оставлен только знак: словесная часть «сбп» в нём чёрная и на тёмной подложке не читается, а название способа и так набрано текстом рядом. Логотип «МИР» достать не удалось — по открытым источникам приходят три разных чужих знака; и «любая карта РФ» шире одной системы, поэтому там нарисована карта |
| 02.09.2026 | **У исламской рассрочки значок — реплика в переписке, а не полумесяц.** Религиозный символ на строке платёжного инструмента подменяет предмет: речь про порядок расчётов, а не про вероисповедание покупателя. Рукопожатие пробовали и сняли — четыре тонких пути на 18px слипались в пятно, похожее на очки |
| 02.09.2026 | **Способы оплаты разобраны из одного предложения ТЗ в пять плашек.** *(Отменено в тот же день — см. строку про состав, заданный владельцем.)* Ответ FAQ про оплату шёл абзацем в шесть строк, и в листе покупки его никто не читал. Каждое слово плашек — из того же предложения, хвост «Подробнее о рассрочке уточняйте у менеджера» стоит под списком дословно. Это перечисление, а не выбор: переключателей нет намеренно — оформить рассрочку на странице нельзя, а элемент, похожий на переключатель, обещает обратное |
| 02.09.2026 | **Форма покупки собирает телефон (обязательно), e-mail (обязательно), Telegram и WhatsApp (нет).** Прямое требование владельца. «Необязательно» написано словом у подписи поля, а не звёздочкой у обязательных: звёздочку надо расшифровывать сноской, слово — не надо |
| 02.09.2026 | **Два согласия, а не одно (Q24).** Обработка ПД и рекламная рассылка — разные основания: ст. 18 закона «О рекламе» требует отдельного предварительного согласия именно на рекламу. Оба сняты по умолчанию — предзаполненная галочка согласием не считается (152-ФЗ). Обязательно только первое. Ссылка на политику стоит в подписи чекбокса, ссылка на оферту — под кнопкой оплаты; пока документов нет, обе рисуются пунктирной надписью, как в подвале |
| 02.09.2026 | **Форма никуда не отправляет и говорит об этом.** Бэкенда в проекте нет по условию, Prodamus — этап 10. При верном заполнении под кнопкой встаёт строка «Данные проверены. Приём оплаты ещё не подключён». Собирать данные, которые некуда положить, хуже, чем не собирать: человек решит, что заявка ушла |
| 02.09.2026 | **Кегль поля — 16px, и это не про красоту.** Safari на iOS увеличивает страницу при фокусе в поле мельче 16px, и лист после этого остаётся в увеличенном виде и уезжает за экран |
| 02.09.2026 | **Ошибка в форме красная, хотя красный на странице отменён (30.08).** Решение 30.08 про акценты и украшения, а ошибка — сигнал, и другого цвета для неё в культуре нет. Взят не токен `blood` (#e10922): на подложке листа он даёт 4.3:1, то есть меньше нормы для мелкого текста. Светлый `#FF7A85` даёт 6.6:1 |
| 02.09.2026 | **Проверка на blur, сводка ошибок сверху формы, фокус в неё.** Правила из набора `ui-ux-pro-max` (Forms): ошибка стоит под своим полем и связана с ним через `aria-describedby`, сводка нужна потому, что кнопка в подвале листа, а проваленное поле может быть выше границы прокрутки. Ввод ошибку не зажигает, только гасит уже показанную. В сводке кнопки, а не ссылки `#id`: якорь внутри модалки меняет адрес страницы и добавляет запись в историю, и «назад» тогда уводит с экрана вместо закрытия листа |
| 02.09.2026 | **Маска телефона поднята из коммита-снимка, а не написана заново** (`lib/phone.ts`, `hooks/usePhoneMask.ts`, удалены 01.09 вместе с формой заявки). Ручная расстановка каретки — не та вещь, которую переписывают: после форматирования React возвращает курсор в конец строки, и позиция ставится в `useLayoutEffect` до отрисовки кадра. Тот же хук работает вторым экземпляром на поле WhatsApp |
| 02.09.2026 | **Мёртвый набор `.fi-*` удалён из `index.css`.** Стили формы заявки пережили чистку 01.09, хотя разметки под них в проекте не осталось (проверено grep). Для листа они не подошли бы всё равно: радиус 12px против 6px по доктрине, всегда горящая синяя рамка и красный `#e10922`. На их месте — набор `.pay-*`, где цвет фокуса приезжает переменной `--pay-accent` из самого листа |
| 02.09.2026 | **В листе показывается прибавка ступени, а не весь состав тарифа.** Владелец: «внеси красивую информацию в модалке по поводу главных преимуществ тарифа». Берётся `addedIn(id, MATRIX_FOLDED)`: у СТАНДАРТА предыдущего нет и выходит его состав целиком (5 позиций), у ПРЕМИУМА прибавка — 6, у ВИПа — 3. Полный список из восемнадцати строк уже стоит в карточке под лентой; второй такой же в листе оплаты никто читать не станет, а вопрос человека здесь — за что он доплачивает. Что ступень стоит на предыдущей, сказано строкой «Плюс всё, что входит в тариф „…“» — без неё прибавка ВИПа читалась бы как весь тариф |
| 02.09.2026 | **Название, описание и значок преимущества берутся из слайда 5 (`learningItem`).** Строка листа выглядит той же сущностью, что карточка в «Как проходит обучение», и человек её узнаёт. Где пункта слайда 5 нет (модули, бонусный модуль, консультации, живая встреча) — название из таблицы тарифов, значок из `FALLBACK_ICONS`, описания нет вовсе: в сетке заказчика его тоже нет |
| 02.09.2026 | **У ключа `feedback` описание не показывается.** Он висит сразу на двух позициях таблицы (чат и проверка домашних заданий), и текст слайда 5 описывает обе разом — «в чатах и при проверке домашних заданий». На СТАНДАРТЕ домашних заданий нет, и описание обещало бы то, чего в тарифе не куплено. Остаются точные названия из таблицы |
| 02.09.2026 | **Значки в списке сверяются рисунком, а не именем компонента.** У бонусного модуля стоял `TacticsIcon` — в списке ПРЕМИУМА он оказывался прямоугольником поля, тем же, что `SoccerFieldIcon` у «Практики» двумя строками ниже; на 16px это одна картинка. Заменён на мяч |
| 02.09.2026 | **У шапки листа снизу линия `border-white/8`.** Содержимое прокручивается под неё, и без линии обрезанная на полуслове строка читается браком вёрстки, а не краем прокрутки. У подвала такая же линия сверху |
| 02.09.2026 | **Замок прокрутки — `body:has(.pay-dialog[open])`, а не класс из React.** Модальный `<dialog>` делает страницу `inert`, но пальцем она всё равно двигается. Класс пришлось бы снимать в четырёх местах (крестик, Esc, тап мимо, размонтирование), и любое пропущенное заперло бы страницу навсегда; селектор сам следует за `[open]` |

---

## Порядок слайдов и готовность

| № | Слайд | Компонент | Статус |
|---|---|---|---|
| 1 | Первый экран | `Hero.tsx` | ✅ принят владельцем |
| 2 | Для кого? | `AudienceSection.tsx` + `AudienceItem.tsx` | ✅ принят владельцем; 01.09 переосвещён по замечанию «слишком мрачно» — ждёт повторного взгляда |
| 3 | Автор обучения | `CoachSection.tsx` | 🔧 собран, правился по замечаниям владельца |
| 4 | Программа обучения | `ProgramSection.tsx` | 🔧 собран, ждёт просмотра владельцем |
| 5 | Как проходит обучение? | `LearningSection.tsx` | ✅ подача выбрана владельцем («Кабинет»), перенесена в боевую и проверена в браузере |
| 6 | Выбери свой тариф | `TariffsSection.tsx` + `PaySheet.tsx` | 🔧 подача выбрана владельцем («Столбцы»), доработана и перенесена на страницу, проверена в браузере; 02.09 на обе кнопки покупки повешен лист оплаты |
| 7 | Авторитет | `AuthoritySection.tsx` (нет) | ⛔ нет контента; 01.09 владелец велел обойти стороной |
| 8 | FAQ | `FaqSection.tsx` | 🔧 подача выбрана владельцем («Крупная полоса»), перенесена на страницу и проверена в браузере |
| 9 | Финальная цитата | — | ⏸ была собрана и подогнана к FAQ, 01.09 снята со страницы и удалена при чистке. Восстанавливается из коммита `0f33445` |

Порядок работы не совпадает с нумерацией — см. `04-PLAN.md`. Но пока владелец ведёт
по порядку слайдов, идём по порядку.

---

## Состав страницы сейчас (`src/App.tsx`)

```
Hero → AudienceSection → CoachSection → ProgramSection → LearningSection →
TariffsSection → FaqSection → SiteFooter
```

**Слайд 2 держит одна секция.** Отдельная обложка (`AboutSection`) давала 288px, из
которых 240 были пустым тёмным стадионом под заголовком — почти треть экрана без
единого слова. Теперь:

- `AudienceSection.tsx` — **весь слайд 2**: снимок стадиона фоном всей секции,
  заголовок «ДЛЯ КОГО?» поверх него, лента из четырёх постеров, точки и кнопка.
  Отдельной обложки-полосы под заголовком нет: высоту задаёт сам заголовок, а до
  ленты идёт обычный `mt-6` (24px) — тот же ритм, что в «Программе» перед списком
  модулей. Блоки с `min-h` (188 → 136 → 88px) отсюда убраны, они давали остаточный
  отступ 58px. Владелец разрешил закрывать ворота карточками: снимок работает фоном
  вокруг ленты. Снимок сдвинут на `top-[-124px]`
- `AudienceItem.tsx` — постер 268×370 (вариант 09 с `/audience.html`): кадр во всю
  площадь, номер, название и описание поверх затемнения. Высота фиксированная —
  текст лежит абсолютно и карточку не тянет. Ширина фиксированная, чтобы край
  соседней карточки было видно и на 360px. Прилипание — `snap-start`, не
  `snap-center`
- Лентой управляют настоящие кнопки: две стрелки и четыре точки, тач-цель 44px.
  Раньше точки были `span` с `aria-hidden` — зона 20×6px, не реагировали ни на что,
  а мышью горизонтальную прокрутку без shift+колеса не сдвинуть, и лента выглядела
  заклинившей
- `AboutSection.tsx` — со страницы снята, файл оставлен в репозитории
- `src/data/audience.ts` — 4 сегмента ТЗ, у всех четырёх есть фотография (Q6 закрыт
  30.08 третьей партией кадров). Поле `num` сохранено намеренно: на него опирается
  макетная `src/dev/AudienceVariants.tsx`, иначе она перестаёт компилироваться
- `src/data/about.ts` (иконки-опоры курса) больше не используется, но не удалён

**Слайд 3 — `CoachSection.tsx`**, стоит между «Для кого» и «Программой»:

- Подача 01 «Досье на поле» с макетной `/coach.html` — выбор владельца из
  восьми блоков. Кадр в полный рост лежит подложкой всей секции, а не
  вставкой внутри неё; надзаголовок, имя и шесть регалий с иконками стоят в
  левой тёмной колонке снимка. Геометрия ТЗ «справа его фото, а слева текст»
  выполняется сама собой, без границы между колонками и без единой рамки
- Кадр — `src/assets/coach-stand.webp` (860×1075, 43 КБ) из присланного
  31.08 `docs/newbg.png` (1122×1402), просто уменьшенный под ретину. Фигура
  стоит в правой половине, левые 55% ширины — чистая темнота. Это не фон, к
  которому пришлось приспосабливаться, а готовая сетка
- **Лицо осветлено, не трогая ни вёрстку, ни положение кадра.** Верхняя
  шторка стыка накрывает голову и гасить её нельзя (пробовали увести стык
  внутрь файла — фигура опустилась, разметка съехала, владелец откатил).
  Поэтому сделано двумя правками, ни одна из которых не двигает пиксель
  раскладки: (1) в самом файле осветлена одна зона — эллипс 205×255 с центром
  (810, 205) по исходнику, кривая `linear(1.26, 8)`, растушёвка до нуля, чтобы
  не было ореола; средняя яркость лица в файле 118 → 151, выбито в белый 24px
  из 34 тысяч; (2) горизонтальная шторка отпущена раньше — полная плотность
  до 57% ширины (текстовая колонка), ноль к 84%, вместо прежних 64% и 94%.
  Итог на странице: 74.5 → 106
- **Снимок растягивается по высоте секции** (`object-cover`, якорь 78% по
  горизонтали). Фигура всегда во весь рост секции, шесть регалий идут вдоль
  неё. До этого кадр масштабировался по ширине и на 360px кончался на
  четвёртой строке — шестая висела ниже ботинок, владелец это и увидел.
  Якорь 78%, а не 100%: при растяжении по высоте кадр шире экрана, и с якорем
  у правого края срезался бы весь пустой левый угол — фигура уезжала бы влево
  (замер на 360: левый край фигуры на 27% ширины). На 78% срезается и справа
  тоже, стадион вместо фигуры. Плата: на узком экране кадр приближен сильнее,
  лицо крупнее, а нижние строки списка частично ложатся на брюки — там темно,
  и шторка добавляет плотности книзу
- Три слоя поверх кадра: горизонтальная шторка держит левую колонку (без неё
  текст лежит на подсвеченной кромке газона), верхняя стыкует секцию с «Для
  кого» цветом `#0c141d`, нижняя гасит кадр в `#0b121a`. Оба стыка сделаны
  цветом, а не прозрачностью: под подложкой сразу тон страницы, растворять
  нечего. Цвета взяты из тональной базы `CourseBackdrop` — цвет в цвет
- Регалия — Bebas-капслок 15px, пояснение 12px, иконка 36px. Колонка узкая,
  прежние 21px в неё не влезают
- У каждой регалии своя предметная фотография из листа `docs/icon.png`
  (`src/assets/ach-*.webp`, 176×176 под ретину). Углы вырезаны альфой прямо в
  файле: на листе за скруглениями лежит его собственный фон. Границы плиток
  сняты замером — сетка 3x2, плитка 347px, углы (134, 97) и (134, 559)
- Своего фона у секции нет: она стоит в той же обёртке, что «Для кого» и
  «Программа», и её накрывает общий свет `CourseBackdrop`. `z-10` — на самой
  секции, не на внутреннем контейнере (та же грабля с `data-reveal`)
- Мёртвое после переезда: маски `.coach-portrait` и `.coach-scene-*` из
  `index.css` удалены; файлы `coach-portrait.webp`, `coach-portrait-side.webp`,
  `coach-band.webp` остались в assets, но не используются никем.
  `coach-scene.webp` живёт на макетных вариантах 04 и 07

**Слайд 4 — `ProgramSection.tsx`**, последняя секция страницы:

- Аккордеон на шесть блоков — та же механика, что была: высота панели
  анимируется по замеру `ResizeObserver`, открыт всегда ровно один модуль,
  по умолчанию первый. Заказчик одобрил аккордеон в ТЗ дословно, поэтому
  раскладку не трогали
- Карточка собрана по формату ТЗ и в его порядке: шапка (номер, название,
  счётчик уроков) → раскрывающийся список уроков → **результат, видимый
  всегда**. Результат стоит снаружи панели раскрытия — это принципиально:
  закрытая карточка обязана отвечать, что человек получит
- **Два состояния карточки.** С утверждёнными уроками (модули 1 и 2) — шапка
  это `<button>` с `aria-expanded`, справа счётчик в два этажа. Без уроков
  (модули 3, 4, 5 и бонус) — шапка обычный заголовок, нажимать не на что,
  на месте счётчика прочерк, а вместо списка строка «Программа модуля
  утверждается». Переключается само по длине `lessons`: придут уроки — блок
  станет раскрывающимся без правки вёрстки
- Бонусный блок помечен тёплым `#FFC14A`: рамка, отсвет под словом и само
  слово «БОНУС» на месте номера. В ТЗ он не пронумерован, а в тарифах входит
  только в ПРЕМИУМ и ВИП
- `src/data/program.ts` — дословные тексты ТЗ и ничего больше. Ни картинок,
  ни хронометража, ни форматов урока. Черновик прежней программы лежит в
  `src/dev/programDraft.ts` и нужен только макетной `/program.html`
- Кнопка «Посмотреть тарифы» в конце — `.btn-hero-turf`, как «К программе» на
  слайде 2. Ведёт на `#tariffs`, которого пока нет: слайд 6 не сделан, в коде
  стоит `TODO`
- Своего фона у секции нет, `z-10` — на самой секции (та же грабля, что в
  «Тренере»). Сход в `bg-pitch` рисует собственный слой внизу секции: её низ
  ездит вместе с аккордеоном

**Слайд 8 — `FaqSection.tsx`**, последняя секция страницы:

- Подача 03 «Крупная полоса» с макетной `/faq.html` — выбор владельца из
  трёх. Вопрос Bebas-капслоком 17px, разделители — волосяные линии, у
  раскрытого вопроса слева цветная нить в 2px, плюс без кружка в 26px
- Открыт ровно один вопрос, первый — сразу. Номеров у вопросов нет
- Высота раскрытия анимируется по замеру `ResizeObserver` — тот же приём,
  что у панели уроков в «Программе»
- Своего фона нет, `z-10` — на самой секции. Сход в `bg-pitch` перед
  подвалом рисует у себя внизу: он всегда у последней секции страницы
- `src/data/faq.ts` — семь вопросов и ответов дословно из ТЗ. Правки только
  типографские: «ё» и кавычки-«ёлочки». Три ответа повторяют сказанное выше
  по странице (бронь за 10 000 ₽, обратная связь по тарифам, скидки клубам) —
  при правке править парой, иначе человек прочтёт разное в двух местах

`AboutSection` и `LeadSection` лежат в репозитории, но со страницы сняты.

---

## Что важно знать про код, чтобы не наступить дважды

Полный список грабель — в `docs/HANDOFF.md`, раздел «Грабли, на которые уже
наступили». Самое частое:

- ~~**`data-reveal` нельзя вешать на элемент с меняющимся `className`**~~ —
  неактуально с 31.08: анимация появления снята со страницы целиком (см.
  «Анимация появления» ниже).
- **Заголовки залиты через `background-clip: text`.** У «Й» пропадает бревис, если
  трогать `line-height` или вертикальные отступы `.section-title` / `.hero-title`.
  Компенсация уже стоит в `index.css`, ломать её нельзя.
- **Отступ между секциями — 52px**, задаётся классом `.section-rhythm`. Мерить надо
  по краске, а не по границам блоков, и только после прокрутки всей страницы:
  непроявленные `data-reveal` смещены на 24px и врут в замерах.
- **Трекинг Bebas — только целыми пикселями.** Токены `--tracking-title` и
  `--tracking-display`. В `em` шрифт замыливается.
- **`data-reveal` делает секцию контекстом наложения.** Он ставит `transform`, а
  любой transform, кроме `none` (включая `translateY(0)` в `.is-in`), отрезает
  внутренние z-index от внешних. `z-10` на контейнере внутри секции после этого
  бесполезен: общий свет `CourseBackdrop` (`z-[3]`) ложится поверх всей секции и
  глушит цвета — замер по пикселям кнопки давал зелёный 151 вместо 176, белый текст
  199 вместо 244. **z-index надо ставить на саму секцию**, как в «Программе».
- **`<main>` не должен становиться прокручиваемым.** У него `overflow-hidden`, и
  любой абсолютный слой, вылезший за низ внутренней обёртки, делает его внутренним
  скролл-контейнером. Браузер тогда прокручивает по якорю **его**, а не страницу:
  «К программе» уезжала внутри `main`, и вернуться к первому экрану колесом было
  нельзя. Лечится `overflow-hidden` на обёртке вокруг секций.
- **Плавная прокрутка (`scrollTo({behavior:'smooth'})`) не работает вместе с
  `snap-mandatory`.** Прилипание тянет ленту назад на каждом кадре анимации: клик по
  «вперёд» давал 15px вместо 280, три клика подряд — 0. Программную прокрутку делать
  присваиванием `scrollLeft`. Свайп пальцем анимируется сам и не страдает.
- **Индекс активной карточки нельзя брать только из события прокрутки.** Оно
  приходит на следующем такте, и быстрые нажатия по стрелке считали текущей
  предыдущую карточку. `goTo` ставит индекс сразу, `onScroll` — догоняет.
- **Tailwind не видит новые файлы, появившиеся после старта dev-сервера** — даже с
  директивой `@source "./"`, которая уже стоит. Проявляется молча: класс просто не
  генерируется. На слайде 6 из-за этого `size-[13px]` не существовал, и все галочки
  в карточках тарифов имели нулевую ширину — на экране их не было вовсе, а в разметке
  всё на месте. Завели новый файл — перезапустите `npm run dev`.
- **Проценты в `padding` считаются от содержащего блока, а не от самого элемента.**
  У ленты тарифов стоит `-mx-5`, то есть она на 40px шире секции, и честное
  `calc(50% - 131px)` давало поле на 20px короче нужного: крайние карточки не
  доезжали до центра (замер на 360: 22px слева против 61 справа). В формулу нужна
  поправка на вылет — `calc(50% + 20px - 131px)`, см. `SIDE_PAD` в `TariffsSection`.

---

## Анимация появления — снята

31.08 владелец попросил убрать всплытие блоков при скролле. Со страницы удалены
атрибуты `data-reveal`, класс `.reveal-child`, проп `index` у карточки модуля и
вызов `useReveal` в `App.tsx`. Сам хук `src/hooks/useReveal.ts` остался в
репозитории, но не вызывается ниоткуда.

**Возврата одним движением не будет:** без CSS-правил хук просто вешает класс,
на который ничто не реагирует. Чтобы вернуть, нужны и правила, и атрибуты.
Удалённый CSS — дословно:

```css
[data-reveal] {
  opacity: 0;
  transform: translateY(24px);
  transition:
    opacity 0.6s var(--ease-mass),
    transform 0.6s var(--ease-mass);
  transition-delay: var(--reveal-delay, 0ms);
}

[data-reveal].is-in {
  opacity: 1;
  transform: translateY(0);
}

/* Для элементов, чей className меняется по состоянию React: useReveal ставит
   .is-in через classList, а React при следующем рендере переписывает атрибут
   целиком, и элемент возвращается в opacity: 0. */
.reveal-child {
  opacity: 0;
  transform: translateY(24px);
  transition:
    opacity 0.6s var(--ease-mass),
    transform 0.6s var(--ease-mass);
  transition-delay: var(--reveal-delay, 0ms);
}

[data-reveal].is-in .reveal-child {
  opacity: 1;
  transform: translateY(0);
}

@media (prefers-reduced-motion: reduce) {
  .reveal-child { transition: none; }
}

@media (prefers-reduced-motion: reduce) {
  [data-reveal] { transition: none; }
}
```

Заодно ушла грабля, которая жила в проекте с самого начала: `data-reveal` нельзя
было вешать на элемент с меняющимся `className`, иначе блок исчезал при
перерисовке. Карточка модуля в «Программе» держалась на `.reveal-child` именно
поэтому.

---

## Чего ждём от заказчика

Обновлять по мере поступления.

| Что | Для слайда | Статус |
|---|---|---|
| Скан настоящей подписи | 1 | ⏳ ждём |
| ~~Портрет анфас, не как в герое~~ | 3 | ✅ прислан 31.08 — `docs/3sl.png` |
| Уроки модулей 3, 4, 5 и бонусного | 4 | ⏳ обещаны к 2–4 сентября. Место под них готово: добавляются строками в `src/data/program.ts`, вёрстку трогать не нужно |
| Результат модуля 4 «Планирование подготовки» | 4 | ⏳ ждём — единственный блок, у которого в ТЗ есть только название |
| Визуальный ряд на 11 пунктов + логотипы NANOFOOTBALL и SCOUTWAY | 5 | ⏳ ждём |
| Номер WhatsApp и три стартовых сообщения | 6 | ⏳ ждём |
| Политика обработки ПД, оферта, реквизиты продавца (Q24) | подвал | ⏳ ждём — место в подвале готово, ссылки включаются заполнением `src/data/legal.ts` |
| Решение по лицензии на образовательную деятельность (Q25) | все | ⏳ ждём — от схемы зависят формулировки на странице |
| Цитаты, вырезки, фото карьеры | 7 | ⏳ ждём |
| ~~Мотивационная цитата и фото~~ | 9 | ✅ не понадобилось: 01.09 владелец дал текст с gadjiev.pro и назвал кадр `coach-band.webp` |
| Решение: микрогруппы в СТАНДАРТЕ? живая встреча вообще? | 6, 8 | ⏳ ждём — живая встреча обещана в ответе 3 FAQ, правится парой с составом ВИП |
| Год старта: «первое ноября» какого года (Q21) | 8 | ⏳ ждём — в текст FAQ год не попадает, слайд не блокирует |

---

## Макетные страницы

**Удалены 01.09 вместе с остальным мусором** — все девять (`/audience.html`,
`/coach.html`, `/program.html`, `/learning.html`, `/tariffs.html`, `/faq.html`,
`/quote.html`, `/buttons.html`, `/variants.html`) и каталог `src/dev/`. Восстановить
можно из коммита-снимка `0f33445`, но обычно не нужно: какие подачи примерялись, чем
различались и почему выбрана та, что стоит на странице, записано в журнале ниже и в
комментариях самих компонентов.

Понадобится новая макетная — заводится парой «`имя.html` в корне + `src/имя.tsx`» и в
прод-сборку не попадает: Vite по умолчанию берёт единственной точкой входа
`index.html`.

## Журнал

Одна строка на сессию: что сделано, что решено, где остановились.

- **30.08.2026.** Разобрано ТЗ (`docs/tz.docx`) и тарифная таблица из Google Sheets.
  Заведены `docs/tz/00-INDEX.md`, `01-SPEC.md`, `02-TARIFFS.md`, `03-QUESTIONS.md`,
  `04-PLAN.md` и этот файл. Кода не трогали. Начинаем слайд 1.
- **30.08.2026.** Слайд 1 сделан в `Hero.tsx`: новое имя академии одной строкой,
  заголовок-обещание в 5 строк (32px, `tracking-title`, без цветового акцента — решили
  не трогать красный вовсе), подзаголовок из ТЗ, кнопка одна и во всю ширину, ведёт на
  `#program`. Подпись оставлена как есть с `TODO` на Q1/Q2 (ждём скан настоящей).
  `npm run typecheck` зелёный. Дальше по ходу: растянули блок по вертикали (отступы
  + leading), опустили текст на 36px, чтобы не налезал на лицо ГМ (`pt-[132px]` →
  `pt-[168px]`), выделили «Гаджи Гаджиева» в надзаголовке весом+непрозрачностью.
  **Владелец принял: «Мне нравится».**
- **30.08.2026.** Первая попытка слайда 2 отвергнута и откачена. Причина, как
  выяснилось: под «поменяем наполнение» имелся в виду только контент, а я заодно
  переделал карточку — вместо зигзага сделал фото широкой полосой сверху.
  **Урок: наполнение ≠ раскладка.**
- **30.08.2026.** Слайд 2 сделан заново: `data/audience.ts` под 4 сегмента ТЗ,
  заголовок «ДЛЯ КОГО?» один на обе секции, кнопка «К программе» в конце
  AudienceSection, общий `AudienceItem.tsx`. Сперва сохранили зигзаг — владелец
  отверг и его.
- **30.08.2026.** Карточка переделана в «широкую строку» — тоже отвергнута. Итого три
  отказа подряд, и все три раскладки были структурно одним и тем же: вертикальный
  список строк, менялись только пропорции кадра и текста. Владелец уточнил:
  **фотографии подходят, не нравится UI блока.**
- **30.08.2026.** Через скилл `ui-ux-pro-max` (Editorial Grid, Exaggerated Minimalism,
  Bento Grid) сегмент переделан из строки списка в **главу**: кадр в вылет за края
  экрана, крупный номер акцентным синим. Тоже отвергнуто — четвёртый отказ подряд.
- **30.08.2026.** Владелец попросил применить правила мобильных приложений и собрать
  несколько вариантов на отдельной странице. Заведена `/audience.html` с пятью
  подачами (см. «Макетные страницы»). Правила взяты из `references/pro-rules.md`
  скилла: тач-цель ≥44px, отклик на нажатие без сдвига раскладки, поверхности
  отделены от фона, разделители читаются в тёмной теме, `aria-selected`/`aria-expanded`
  у контролов, ритм отступов кратен 4. `--design-system` по запросу «sports coaching
  education mobile app» промахнулся (выдал детские приложения, Claymorphism, Baloo 2,
  «Avoid: Dark modes») — не применялся. Боевые компоненты пока остались на варианте
  «глава». `npm run typecheck` зелёный. **Ждём выбора владельца.**
- **30.08.2026.** По просьбе владельца на ту же страницу добавлены варианты 06–10
  (первые пять не тронуты): плитки 2×2 с раскрытием в ряд, истории-кружки, где фото
  работает переключателем, фон-переключатель с кроссфейдом кадров, постер-свайп,
  нижний лист с затемнением и Esc. `npm run typecheck` зелёный.
- **30.08.2026.** Владелец выбрал **вариант 02 (свайп с пагинацией)** — перенесён в
  боевые компоненты. Из-за одного трека карусели разбиение 2+2 по секциям снято:
  AboutSection стала обложкой с заголовком, вся лента и кнопка живут в
  AudienceSection. `npm run typecheck` зелёный. Макетная `/audience.html` со всеми
  десятью подачами оставлена. **Ждёт визуального просмотра владельцем.**
- **30.08.2026.** Слайд 2 пересобран по просмотру в браузере. Карточка переведена с
  варианта 02 на **09 (постер-свайп)**: кадр во всю площадь 268×370, текст поверх
  затемнения, описание поднято с 11.5px до 12.5px (на фотографии мелкий кегль теряется
  сильнее, чем на панели). Обложка и лента слиты в одну секцию (**раскладка B**):
  снимок стадиона стал фоном всего блока и уходит под карточки, сдвинут на -88px,
  чтобы ворота и газон попали в полосу над лентой. AboutSection снята со страницы,
  файл оставлен. `CourseBackdrop` мерил высоты по `#about` — переведён на снимок
  (`data-stadium`) и `offsetTop + offsetHeight` секции. Заодно найден и починен
  реальный баг: `main` был прокручиваемым (scrollHeight 4227 против clientHeight
  2597) из-за слоя `CourseBackdrop` высотой 3600px, и якорь «К программе» двигал
  содержимое внутри `main`, а не страницу. Высота слайда 2 упала с 773 до 716px,
  блок помещается в один экран. `npm run typecheck` зелёный, горизонтального скролла
  нет на 430 и на 360. **Ждёт визуального просмотра владельцем.**
- **30.08.2026.** Владелец посмотрел и указал на три вещи: блок уезжает вниз, текст
  на постере нечитаем, «слайды не двигаются». Все три подтвердились замерами.
  Управление лентой было мёртвым: точки — `span` с `aria-hidden` и зоной 20×6px,
  мышью лента вообще не листалась. Сделаны стрелки и точки-кнопки с тач-целью 44px,
  прилипание переведено с `snap-center` на `snap-start`, программная прокрутка — не
  `behavior:'smooth'` (дралась со snap), индекс ставится сразу в `goTo`. Затемнение
  постера уплотнено, описание поднято до 13px. Полоса стадиона над лентой ужата с
  188 до 88px — владелец разрешил закрыть ворота карточками. Проверено кликами:
  0 → 280 → 560 → 733, стрелки гаснут на краях, точки работают. Высота слайда 654px
  на 360. `npm run typecheck` зелёный.
- **30.08.2026.** Отступ «ДЛЯ КОГО?» → лента приведён к ритму проекта: блок с `min-h`
  убран, у ленты `mt-6`. Было 58px остаточных, стало 24. Высота слайда 620px на 360.
- **30.08.2026.** Заведена макетная `/buttons.html` (`src/dev/ButtonStyles.tsx`):
  шестнадцать подач CTA, разложенных по весу — от главной кнопки экрана до текстовой
  ссылки. Каждая на 430 и 360px, с пометкой, под какой слайд. Всё на токенах проекта,
  в `index.css` ничего не добавлено. **Ждём выбора владельца, какую кнопку в какой
  слайд.** Три грабли по ходу: Tailwind не отдаёт классы из только что созданного
  файла до перезапуска dev-сервера (в том числе `min-h-*` — кнопки были 20px вместо
  48); `font-['Bebas_Neue_Cyrillic',...]` как arbitrary-утилита не разбирается, имя
  шрифта надо задавать инлайновым стилем; `justify-center` в общей строке классов
  дерётся с `justify-between` в частной — побеждает порядок правил в собранном CSS,
  а не в атрибуте.
- **30.08.2026.** Первая версия `/buttons.html` отвергнута: «это не CTA, а обычные
  статичные кнопки, все одинаково голубые». Верно — менялась геометрия, а материал у
  всех шестнадцати был один. Страница переписана по двум осям: **палитра** из восьми
  цветов на одной форме (электрик, небесный, газон, лайм-неон, золото, индиго, циан,
  графит — красного нет по решению от 30.08) и **19 подач**, где отличается материал:
  кадр стадиона в заливке, полосы газона с линией штрафной, металл с бликом, неон,
  стекло, нижняя грань с проседанием при нажатии, заливка-развёртка, едущая стрелка,
  билет с перфорацией, градиентная рамка, портрет ГМ в кнопке, прогресс набора прямо
  в заливке. Стили вынесены в один `<style>` внутри макетного компонента: половина
  держится на псевдоэлементах и keyframes, утилитами это не выражается. Грабля:
  квадратный вырез под лицо на `hero-gadzhiev.webp` через `object-position` не
  делается — исходник почти квадратный, запас по горизонтали 7px, в кадр попадали
  прожекторы; нужен фон с `background-size: 390%` и точкой 72% × 19%.
  **Ждём выбора владельца: цвет + номера подач по слайдам.**
- **30.08.2026.** Кнопка «К программе» на слайде 2 переведена с плоской синей на
  подачи с макетной. Сперва примерян **вариант 02** (кадр стадиона в заливке): на
  проектном фоне ушёл в тон секции, а после подъёма множителя до электрика стало
  видно, что кнопка повторяет ту же фотографию, что лежит фоном секции. Снят. Взят
  **вариант 03 «разметка поля»** — `.btn-hero-turf` в `index.css`: полосы под 115°
  с шагом 36px, линия штрафной через `::after`, зелёный отделяет кнопку от синей
  секции. Иконка — стрелка, а не свисток с макетной: по ТЗ кнопка переносит на
  четвёртый слайд, это направление, а не действие. Всё градиентами, вес нулевой.
  `npm run typecheck` зелёный, горизонтального скролла нет на 430 и 360.
- **30.08.2026.** Кнопка слайда 2 доведена: заливка выцветала книзу — градиент шёл с
  `#1aa059` в `#10653a`, падение светлоты на треть на высоте 50px, и нижняя половина
  теряла цвет. Диапазон сжат до `#21b365 → #17954f`, верх светлее только ради объёма.
  Белая линия штрафной по низу убрана: на макетной читалась деталью, на живой
  странице — подчёркиванием под подписью.
- **30.08.2026.** Владелец: «кнопка всё равно теряет цвет». Замер по пикселям показал
  зелёный 151 при заявленном 176 и белый текст 199 вместо 244 — поверх всей секции
  «Для кого» лежал слой `CourseBackdrop`. Причина: `data-reveal` ставит `transform`,
  секция становится контекстом наложения, и `z-10` на внутреннем контейнере перестаёт
  работать. `z-10` перенесён на саму секцию (в «Программе» так уже было сделано по
  этой же причине). После подъёма вылезли два следствия, оба починены: нижняя шторка
  секции держала плотность .74 до самого низа и гасила её в (6,10,15) — теперь
  отпускается к 430px; свет под стыком включался в первом же пикселе программы
  (скачок с (6,10,15) до (25,40,63)) — добавлена маска, разгорается за 190px.
  Проверено по краске: поперёк границы 12,20,29 → 13,21,31 → 14,21,32, скачка нет.
- **30.08.2026.** Заказчик прислал третью партию кадров (`docs/1–4.png`, вертикальные
  1122×1402). **Q6 закрыт** — фотография любительских команд наконец есть, заглушка
  со свистком удалена, `image` снова обязательное поле. Раскладка: 2 → юношеские,
  3 → любительские, 4 → профессиональные, 1 → игрок в тренеры. Переведены в webp по
  900px, 32–41 КБ вместо PNG по 1,5 МБ. Вертикальный кадр входит в постер 268×370
  почти целиком (обрезается 28px по горизонтали), поэтому зумы из прежней партии
  убраны — они были нужны, чтобы вытаскивать героя из горизонтального общего плана.
  Тексты слайда 2 сверены с `01-SPEC.md` посимвольно: все четыре заголовка и все
  четыре описания совпадают.
- **30.08.2026.** Слайд 2 закрыт. Финальные правки по замечаниям владельца: номера
  сегментов (01–04) убраны с карточек — поле `num` в данных осталось, оно ключ списка
  и на него опираются макетные страницы; тексты выровнены по общей верхней отметке
  172px вместо прижатия к низу — блоки начинались на 177, 194, 215 и 177px, и у
  «Тренеров профессиональных команд» текст уезжал вниз на 40px, теперь у всех 186px.
  Стопы затемнения подняты под новую отметку. **Слайд 2 принят владельцем.**
  Следующая сессия — слайд 3.
- **30.08.2026.** Слайд 3 «Автор обучения» собран, `CoachSection` вернулась на
  страницу между «Для кого» и «Программой». Раскладка по Q9: надзаголовок, имя,
  портрет во всю ширину, шесть регалий линией карьеры. **Q10 закрыт без заказчика:**
  нужного кадра анфас не было ни в `docs/newassets/` (там `hero.png` и `newhero.png` —
  тот же профиль, что в герое), зато он нашёлся в `src/assets/bg.png` — широком
  баннере, где ГМ стоит справа, скрестив руки, и смотрит в камеру. Взята правая часть
  (760×724), над головой добавлен запас: верхняя полоса кадра отражена, размыта и
  затемнена — по построению шва нет, а макушка перестала упираться в край. Первая
  попытка растянуть верхние 30px давала видимую горизонтальную ступеньку.
  Вводный абзац и золотые 3D-бейджи регалий сняты (см. «Принятые решения»).
  Поле `icon` убрано из `achievements.ts` — иначе шесть неиспользуемых webp
  продолжали попадать в сборку.
  Грабля: `CourseBackdrop` считал `seam` (конец «Для кого») одновременно и началом
  программы. Между ними встала новая секция ~880px, и два нижних световых пятна
  остались под портретом. Добавлен третий замер — `#program`.offsetTop; сам
  «Автор обучения» наблюдать не нужно, его высота входит в этот offsetTop.
  Наблюдать программу по-прежнему нельзя: аккордеон пересчитывал бы фон покадрово.
  `npm run typecheck` и `npm run build` зелёные. **Ждёт визуального просмотра
  владельцем.**
- **31.08.2026.** Владелец посмотрел первую версию слайда 3: не нравится. Разбор
  дал три структурные причины, не косметические. Первая: регалии — единственное
  содержание слайда — были набраны 14.5px серым по тёмному, то есть тише всего на
  экране, а громким было имя, которое и так стоит в надзаголовке. Вторая: точка на
  вертикальной черте обещала хронологию, которой в этих шести пунктах нет (клубы
  РПЛ, две премии, учёная степень в ряд не выстраиваются) — структурный приём врал
  о содержании. Третья: форма «надзаголовок → имя → кадр → вертикальный список» —
  ровно та, которую владелец отверг четыре раза подряд на слайде 2.
  Заведена макетная `/coach.html` с шестью структурно разными подачами (см.
  «Макетные страницы»). Заодно проверена геометрия ТЗ «фото справа, текст слева»:
  она упирается не в вёрстку, а в кадр — работает только на полосе 2.15:1, где
  портрет высотой 200px и лицо в нём 70px. Это вариант 04, чтобы владелец увидел
  размен сам. В боевой компонент перенесён вариант 01 «табло почёта».
  Из скилла `ui-ux-pro-max` взяты два правила: «Heading Line Balance» (жёсткие
  `<br>` в заголовках не ставим — фамилия вынесена по смыслу, остальное держит
  `text-balance`) и app-чеклист по тач-целям для интерактивных вариантов.
  `npm run typecheck` и `npm run build` зелёные, наличие классов в собранном CSS
  проверено грепом. **Ждём: какой из шести номеров берём.**
- **31.08.2026.** Владелец: «ты не сделал то, чего просят в ТЗ. Слева текст,
  справа фото». Он прав, и мой вчерашний вывод «на 430px эта геометрия не живёт»
  был неверен — я прочитал «слева текст» как «весь текст слайда слева» и померил
  раскладку, где рядом с кадром должны встать ещё и шесть регалий. В ТЗ следом
  идёт «далее — списком регалии»: слева стоят только надзаголовок и имя. В таком
  чтении всё помещается спокойно.
  Слайд переделан колонками (вариант 07 на макетной, перенесён в боевые). Ширина
  колонок не подобрана на глаз, а посчитана от узкого места: «МУСЛИМОВИЧ» — одно
  слово, переносить некуда, не влезет — вылезет за экран. Разовым разбором `hmtx`
  шрифта получено 153px при кегле 32; кадр взят 44%, тексту остаётся 174px даже
  на 360, запас 21px.
  Под узкую колонку нарезан отдельный кадр `coach-portrait-side.webp`: общий
  портрет в колонке 180px давал голову высотой 70px. Тем же приёмом, что и
  первый, — отражённая и размытая полоса над головой вместо упёртой макушки.
  На макетной вариант 04 переименован в «раскладку ТЗ одной фотополосой» и
  оставлен вторым прочтением — рядом с 07 видно, почему колонки сильнее.
  `npm run typecheck` и `npm run build` зелёные, классы в собранном CSS
  проверены. **Ждём: какой из семи номеров берём.**
- **31.08.2026.** Владелец прислал съёмочный кадр (`docs/3sl.png`: ГМ на поле
  ночного стадиона, смотрит в камеру) и выбрал **вариант 04 «одной
  фотополосой»** вместо стоявшего варианта 07. Условие сверх подачи: фон
  страницы должен идти сквозь секцию бесшовно — «размыть верхнюю и нижнюю
  границу ассета, примерно как бекграунд второго слайда».
  Собран `src/assets/coach-scene.webp`: канва наращена вверх на 250px, чтобы
  верхнему растворению было где идти (голова на исходнике начинается на 39-м
  пикселе), полоса — отражённый, размытый и притемнённый верх кадра, поверх
  зоны положена заливка тоном страницы. Первая сборка без заливки давала на
  стыке светлую линию: маска проявляла размытое зарево прожекторов — видно на
  замере яркости по скриншоту (20 → 38 по границе справа).
  Растворение краёв — парой `.coach-scene-sharp` / `.coach-scene-blur`, тем же
  приёмом, что стадион на слайде 2, но размытая копия отдана только верху:
  внизу в кадре пола пальто, и `scale(1.07)` двоил бы её край. Стопы масок в
  процентах — снимок масштабируется вместе с экраном, пиксельные стопы на
  360px резали бы макушку.
  Заодно вылез и починен стык со слайдом 2: его собственный тон `#0c141d`
  обрывался ровно по границе секции (яркость 20 → 9), теперь гаснет к низу
  секции и общий свет проходит сквозь него.
  Проверено плейврайтом на 430 и 360: горизонтального скролла нет
  (`scrollWidth == clientWidth`), стыки сверху и снизу без ступеньки — замер
  по пикселям скриншота, а не на глаз. `npm run typecheck` и `npm run build`
  зелёные, классы и ассет в собранном бандле проверены грепом.
  **Ждём просмотра владельцем.**
- **31.08.2026, продолжение.** Владелец: «сверху есть стык и он сильно виден»
  и «приблизь Гаджи, он сильно отдалён». По стыку я был неправ дважды за
  сессию: сначала решил, что виновата секция «Для кого», и погасил её тон к
  низу — а тональная база `CourseBackdrop` стартует ровно тем же `#0c141d` на
  границе секций, то есть до моей правки цвета стыковались цвет в цвет. Своей
  «починкой» я открыл под секцией `bg-pitch` и сделал стык видимым. Откачено.
  Настоящая причина нашлась замером: верх кадра был залит `#05080b`, темнее
  фона на 12 единиц яркости, и снимок проявлялся тёмной полосой — спад 19 → 7
  укладывался в 10px. Заливка перекрашена в `#0c141d`, спад стал плавным на
  60px, линии нет (проверено профилем яркости по скриншоту с шагом 2px).
  Приближение: 1.29x, окно 150..1450 исходника. Запас взят справа — за
  фигурой лежало 264px пустого газона с воротами, а тёмная левая половина под
  текст сохранена целиком. На 360px имя сбавлено до 30px, иначе «МУСЛИМОВИЧ»
  упирается в пальто. `npm run build` зелёный.
- **31.08.2026, третий заход.** Владелец: «сделай отступ как у других двух,
  слайд слишком опущен» и «в docs/icon все шесть иконок одним файлом, нарежь».
  Отступ: текст переставлен с низа кадра на верх. Считать нужно было не по
  кадру, а по соседям — надзаголовок стоял на 137px от верха секции, тогда
  как заголовки «Для кого» и «Программы» стоят на 14 и 26. Теперь 30.
  Следом вылезла дыра: текст ушёл наверх, и под именем осталось 240px пустого
  газона слева. Кадр укорочен по 727-ю строку исходника (860×600, 300px на
  430) — низ снимка теперь приходит примерно туда же, где кончается имя.
  Иконки: лист 1536×1024 нарезан на шесть плиток. Границы не подбирал —
  сначала по локальной дисперсии найдены полосы с содержимым, потом по
  профилю пикселей уточнены края крайних плиток: сетка 3x2, плитка 347px,
  углы (134, 97) и (134, 559). Углы скруглены альфой в самом файле, иначе за
  ними тянется фон листа. Порядок плиток на листе совпал с порядком регалий.
  Проверено на 430 и 360, `npm run typecheck` и `npm run build` зелёные.
- **31.08.2026, четвёртый заход.** Владелец: «ассет слишком низко по отношению
  к имени». Причина — запас неба над макушкой: его считали под прежнюю
  раскладку, где текст стоял по низу кадра, и после переезда текста наверх
  60px пустого неба вклинились между верхом секции и фигурой. Запас
  пересчитан от текста: имя занимает 50..144px от верха секции, макушка
  выведена на 50px — 112px исходника вместо 180. Кадр стал 860×555 (278px на
  430), маски подтянуты под новые ориентиры (небо 0..13%, макушка 18%).
  Лицо и имя теперь стоят на одной высоте. Стыки перепроверены профилем
  яркости: сверху плавно, снизу ровно 17–18 без ступеньки. Сборка зелёная.
- **31.08.2026, пятый заход.** Три правки подряд по выравниванию кадра и имени
  владелец забраковал: «не выровнял вообще», потом «ассет сжался, блок
  маленький». Гадать перестал — показал три раскладки на выбор, владелец взял
  «имя по центру кадра, лицо и имя на одной линии».
  Под неё пересобран кадр: центр лица выведен в центр полосы (PAD + 184 =
  (PAD + H) / 2, при H = 700 даёт PAD = 331), текстовый блок центрируется в
  той же полосе. Полоса осталась крупной — 341px на 430.
  Отражённая полоса над головой из подпорки стала содержанием: яркость
  поднята с 0.42 до 0.85, и над фигурой читается зарево прожекторов, а не
  пустая чернота. Стыки перепроверены профилем яркости: сверху 13 → 7 плавно,
  снизу ровные 17 через всю границу. Горизонтального скролла нет.
  `npm run typecheck` и `npm run build` зелёные.
  **Урок: когда владелец второй раз говорит «не то» про одно и то же место,
  надо не править в третий раз наугад, а показать варианты.**
- **31.08.2026, шестой заход.** Владелец: «сделай кардинально другие блоки,
  используй иконки регалий, вот кадр в полный рост (`docs/newbg.png`),
  используй мобильную и app-разметку». Макетная `/coach.html` переписана
  целиком: восемь структурно разных блоков вместо семи прежних подач одного
  списка. Приёмы взяты из приложений — нижняя шторка, сегментированный список
  профиля, лента со snap, сетки плиток; правила по тач-целям, состояниям и
  контрасту — из app-чеклиста `ui-ux-pro-max` (`references/pro-rules.md`).
  Осознанное отступление от чеклиста: он требует векторных иконок, а здесь
  растровые — это предметные фотографии заказчика, то есть содержание, а не
  системные значки.
  Новый кадр (1122×1402) даёт то, чего не давали прежние: фигура в правой
  половине и 55% чистой темноты слева. Владелец выбрал вариант 01 «Досье на
  поле» — он и перенесён в боевой компонент.
  При переносе вылезла разница между макетной и живой страницей: в макетной
  блок фиксированной высоты, на живой она растёт от переносов текста, и
  `object-cover` увеличивал кадр на 360px так, что фигура наезжала на текст.
  Масштаб привязан к ширине. Стыки сверху и снизу перепроверены профилем
  яркости: 18 → 18 и 17 → 16, ступенек нет. `npm run typecheck` и
  `npm run build` зелёные.
- **31.08.2026, доводка.** Владелец: «лицо Гаджи затемнено». Замер подтвердил:
  68 по яркости на странице против 140 в файле. Виноваты были две шторки —
  верхняя (стык с «Для кого») накрывала голову целиком, потому что на
  исходнике макушка идёт от 78-й строки, а горизонтальная держала .32 на 62%
  ширины и сходила на нет только к 88%, то есть ровно по лицу.
  Верхняя шторка со страницы убрана, стык уведён внутрь файла: канва наращена
  вверх на 220px и залита `#0c141d` выше макушки. Горизонтальная отпускает
  кадр к 74% и получила вертикальную маску на первые 9% высоты — без неё на
  границе секций вылез скачок 18 → 8 по левому краю. Лицо теперь 130 против
  140 в файле, стыки сверху и снизу без ступенек. Сборка зелёная.
- **31.08.2026, откат доводки.** Правку по яркости лица владелец откатил:
  «верни что было, сильно съехала разметка». Кадр вернулся к прежнему
  (860×1075, без наращенной канвы), обе шторки — к прежним значениям.
  Вывод на будущее: верхняя шторка стыка и положение фигуры в кадре связаны
  жёстко, править одно без другого нельзя. Если владелец вернётся к яркости
  лица, менять придётся раскладку целиком, а не слой.
- **31.08.2026, анимация.** По просьбе владельца снято всплытие блоков при
  скролле: атрибуты `data-reveal`, класс `.reveal-child` и CSS-правила под них
  удалены, вызов `useReveal` из `App.tsx` убран, вместе с ним ушёл ставший
  ненужным проп `index` у карточки модуля. Проверено в браузере: у всех секций
  `opacity: 1` и `transform: none`, узлов с `data-reveal` и `.reveal-child` на
  странице ноль. Хук оставлен в репозитории с пометкой, что он не вызывается;
  текст удалённого CSS записан в этот файл, раздел «Анимация появления».
  `npm run typecheck` и `npm run build` зелёные.
- **31.08.2026, масштаб фигуры.** Владелец: «сделай ГМ чуть больше, чтобы все
  шесть регалий были пропорционально его росту — на телефоне шестая сильно
  ниже него». Кадр переведён с масштабирования по ширине на растяжение по
  высоте секции. Якорь по горизонтали 78%, а не 100%: с правым краем фигура
  уезжала влево и лезла в текст. У горизонтальной шторки появилась
  вертикальная маска — вверху она работает на 34% силы (там лицо), книзу
  выходит на полную (там регалии идут по фигуре). Проверено на 430 и 360:
  ботинки приходят к последней строке, стыки без ступенек (18 → 19 сверху,
  16 → 16 снизу). Сборка зелёная.
- **31.08.2026, яркость лица.** Владелец: «можем осветлить лицо, не меняя
  вёрстку и не двигая бекграунд? нравится, как всё расположено». Сделано так,
  что ни один элемент не сдвинулся: в файле кадра осветлена одна эллиптическая
  зона по голове (растушёванная, чтобы не было ореола), а горизонтальная
  шторка отпущена раньше по ширине — полная плотность держится до 57%, то есть
  до конца текстовой колонки, и сходит на нет к 84%, где лицо. Яркость лица на
  странице 74.5 → 106 при том же положении и масштабе. Фон под нижними
  строками списка остался 56 — текст читается. Сборка зелёная.

- **31.08.2026, слайд 4.** «Программа обучения» собрана по ТЗ. `src/data/program.ts`
  переписан с нуля: шесть блоков дословными текстами, у модулей 1 и 2 — уроки (7 и 5),
  у модулей 3, 4, 5 и бонусного `lessons` пустой, у всех кроме модуля 4 есть результат.
  Черновик из шести выдуманных модулей уехал в `src/dev/programDraft.ts`, чтобы
  макетная `/program.html` продолжала собираться. `ProgramSection.tsx` перебран:
  результат вынесен из панели раскрытия наружу и виден всегда (ТЗ), счётчик уроков
  встал в шапку (Q11), блок без уроков перестал быть кнопкой и пишет «Программа модуля
  утверждается» (Q13), картинки, хронометраж, форматы урока и подзаголовок модуля
  сняты (Q12), бонус помечен тёплой рамкой и словом вместо номера, в конце появилась
  кнопка «Посмотреть тарифы» на `#tariffs` с `TODO` (слайда 6 нет). Итоговая подпись
  «6 модулей, 35 уроков» снята как враньё. `npm run typecheck` и `npm run build`
  зелёные. В браузере не смотрел — жду показа владельцу.
- **31.08.2026, слайд 5, первый заход.** «Как проходит обучение?» собрана лентой
  из четырёх экранов — буквально по разбивке ТЗ и по просьбе «на каждый слайд по
  2-3 фишки». Заведены `src/data/learning.ts`, `LearningSection.tsx` и десять новых
  контурных иконок в общем наборе. `npm run typecheck` и `npm run build` зелёные.
  **Владелец отверг сразу: «не нравится, как всё расположено. Возможно, в ТЗ
  неправильно дали понять суть»** — и попросил рассказать, как я сам вижу блок.
- **31.08.2026, слайд 5, разбор и макетная.** Ошибка была не в оформлении, а в
  чтении содержания. Свайп прятал 8 пунктов из 11 в секции, которая перечисляет
  состав того, за что платят 50–100 тысяч; на странице уже есть лента (слайд 2) и
  аккордеон (слайд 4); одиннадцать одинаковых плиток уравнивали одиннадцать очень
  разных вещей. Плюс сама разбивка ТЗ (3+2+3+3) делит пункты по месту в списке, а
  не по смыслу: они отвечают на три разных вопроса — как устроены занятия, что
  человек делает внутри потока, что остаётся после. Ядро — обратная связь,
  микрогруппы и разборы с Гаджи Муслимовичем.
  Данные перебраны под это: плоский `LEARNING_ITEMS` с ключами и `pickLearning`,
  разбивка ТЗ осталась отдельным экспортом. Заведена макетная `/learning.html`
  (`learning.html` → `src/learning.tsx` → `src/dev/LearningLayouts.tsx`) с четырьмя
  структурно разными подачами: три этажа, цикл обучения, бенто, спецификация.
  Ни одна не интерактивная — в этой секции прятать нечего.
  Побочный вывод по Q14: при делении на этажи иллюстраций нужно три, а не
  одиннадцать. Боевая секция намеренно оставлена как есть — заменим после выбора.
  `npm run typecheck` и `npm run build` зелёные, макетная в прод-сборку не
  попадает. **Ждём выбора владельца.**
- **31.08.2026, слайд 5, третий заход.** Владелец: «вообще не нравится. Задумка
  хорошая, но исполнение плохое по дизайну и UI». Структура (четыре смысловых
  этажа) тем самым принята — забракован материал. Диагноз нашёлся в собственном
  CSS проекта: у `.btn-hero` записано «радиус 6px вместо мягких 14 — язык
  тактических схем, а не дашборда», а я собрал секцию ровно дашбордом —
  скруглённые плашки 14–16px, мягкая синяя подсветка, значки в квадратиках с
  10%-заливкой, названия в 16–20px при заголовках страницы в 32px, и радиальный
  синий отсвет под всем подряд, из-за которого подложка, рамка, значок и текст
  оказались в одном тональном диапазоне.
  Макетная переписана: три подачи одной и той же структуры, радиус 6px или ноль,
  названия Bebas'ом в 21–29px, свет только волосяной линией и кромкой в 1px,
  газон — CSS-приёмом кнопки. `npm run typecheck` и `npm run build` зелёные.
  **Урок: перед показом смотреть отрисовку глазами.** Два отказа подряд по
  дизайну получены вслепую — я ни разу не открыл собранную страницу.
- **31.08.2026, слайд 5, четвёртый заход.** Владелец забраковал и предыдущий
  набор, но задал направление: разбить на категории («как проходит обучение»,
  «что получаем бесплатно»), обязательно выделить онлайн-площадку «Платформа»,
  использовать разные цвета и убрать сгенерированное футбольное поле. Сделано:
  платформа стала героем секции и показывает саму себя (окно кабинета с тремя
  настоящими модулями из `data/program.ts`, полоса прохождения, «24/7» из ТЗ),
  категории закодированы цветом (синий / мята / золото), газон снят.
  **Впервые за все заходы отрисовка проверена глазами** — владелец разрешил
  открыть браузер. Три отказа подряд по дизайну были получены вслепую. По
  скриншотам сразу нашлось и исправлено: тело пункта не отличалось по
  контрасту от названия и одиннадцать пунктов сливались в стену; у всех трёх
  категорий была одинаковая форма строки, и цвет оставался единственным
  различием; «24/7» дублировалось в шапке и в окне; штриховка на золотой
  полосе читалась сигнальной лентой; точка категории в «Маршруте» налезала на
  первую букву подписи. `npm run typecheck` и `npm run build` зелёные.
  **Ждём выбора владельца.**
- **31.08.2026, слайд 5, перенос в боевую.** Владелец выбрал подачу 01 «Кабинет».
  `LearningSection.tsx` переписан под неё, лента по разбивке ТЗ снята целиком.
  Категории с цветами вынесены в `data/learning.ts` (`CATEGORIES`) — макетная
  и боевая секция берут их оттуда, чтобы не разъехаться. Проверено в браузере на
  430 и 360: горизонтального скролла нет, `main` не прокручивается, ни один узел
  не вылезает за ширину секции, стык с «Программой» без ступеньки; секция целиком
  лежит ниже последнего стопа `CourseBackdrop`, где слой уже сплошной `#05080b`.
  Высота 1727px на 430, 1873px на 360. `npm run typecheck` и `npm run build`
  зелёные. Остаются: согласование названий категорий (единственный текст не из
  ТЗ), Q14 (три иллюстрации вместо одиннадцати) и Q15 (логотипы партнёров).
- **31.08.2026, слайд 5, фон.** Владелец: «всё нравится кроме background, он
  стремится к чёрному, а хотелось бы, чтобы он продолжался с блока „Программа
  обучения“». Причина нашлась в `CourseBackdrop`: последним стопом тональной
  базы стоял `#05080b` на `prog+950`, то есть тон умирал на середине программы
  — пока программа была последней секцией, это читалось концом страницы, а с
  появлением слайда 5 стало провалом. Тон доведён до `#0a1119` и держится до
  низа слоя; сход в `bg-pitch` переехал из «Программы» вниз слайда 5; высота
  слоя поднята с 4200 до 5600px, иначе тон обрывался бы ступенькой на границе
  слоя; свет слайда 5 перенесён внутрь секции, чтобы не уезжать от неё при
  раскрытии аккордеона программы. Проверено в браузере на 430 и 360: все три
  стыка без ступеньки, горизонтального скролла нет. Сборка зелёная.
- **31.08.2026, слайд 5, ступенька и цвет.** Владелец: «есть ступенька между
  программой и как проходит обучение» и «сделай фон более интересней в плане
  переливания цветов». Ступенька нашлась замером, а не на глаз: под замеры
  написан минимальный декодер PNG на одном zlib (Pillow в системе нет) —
  `scratchpad/png.py`. Профиль яркости поперёк стыка показал скачок 16.4 →
  21.3 на середине ширины, у краёв ровно. Виноват был свет, который я положил
  внутрь самой секции: радиальные пятна обрезались её верхним краем. Свет
  перенесён в общий `CourseBackdrop` — после переноса скачок ±0.8 на 430 и
  ±1.3 на 360, то есть наклон градиента.
  Переливание сделано цветами категорий в порядке их появления: бирюза, синий,
  лиловый, мята, золото; центры чередуются по горизонтали, чтобы оттенок шёл
  по диагонали. Первый заход был слишком робким (замер: золото и лиловый на
  фоне не проявлялись вовсе) — пятна переставлены внутрь секции, альфа поднята
  и проверена по контрасту: 5.2–5.7:1 для белого текста в 55%. Сборка зелёная.
- **31.08.2026.** Слайд 6 «Выбери свой тариф»: заведены `src/data/tariffs.ts`
  (три тарифа, матрица из 18 позиций, кнопки, три обращения в WhatsApp) и макетная
  `/tariffs.html` с тремя подачами — 01 столбцы лентой, 02 лестница, 03 матрица.
  Данные читались по расшифровке `02-TARIFFS.md`, а не по CSV: в исходной таблице
  «не входит» показано зачёркиванием, при экспорте оно пропадает. Решено нашей
  стороной: 15 кнопок сведены к 5 (Q17), модули 1–5 свёрнуты в одну строку,
  названия позиций приведены к одному виду, ПРЕМИУМ выделен без свечения.
  Добавлен `StarIcon` в `components/icons.tsx`. `npm run typecheck` зелёный,
  горизонтального скролла нет ни в одной подаче на 430 и 360.
  **Ждём выбора подачи владельцем — на живой странице слайда 6 пока нет.**
  Грабля дня: новый файл в `src/dev` Tailwind подхватил только после перезапуска
  dev-сервера — до этого `size-[13px]` молча не генерировался, и все галочки в
  таблицах имели нулевую ширину.
- **31.08.2026.** Слайд 6: владелец выбрал подачу 01 «Столбцы» и заказал доработку —
  «неактивные столбцы чуть прозрачными, у каждого столбца свой цвет с аналогией к
  названию, тарифы идут по возрастанию, хит выделить больше всех». Сделано: шкала
  «сталь → синий → золото» заведена в `data/tariffs.ts` (`accent`), у каждой ступени
  свой приём выделения (ничего / шапка ХИТ / золотая грань), возрастание показано
  делениями, неактивные гаснут на `opacity .52` + `saturate(.6)`, у хита осталась
  единственная плоская синяя кнопка и тень под карточкой. `npm run typecheck`
  зелёный, горизонтального скролла нет на 430 и 360.
  **Дальше: перенос в боевой `TariffsSection.tsx` и якорь `#tariffs`.**
- **31.08.2026.** Слайд 6 перенесён на страницу: `TariffsSection.tsx` стоит последней
  секцией, якорь `#tariffs` заработал, кнопка «Посмотреть тарифы» в конце «Программы»
  доезжает. Вместе с секцией переехал сход в `bg-pitch` (из «Как проходит обучение»),
  слой `CourseBackdrop` поднят до 7200px, под саму секцию добавлены два цветных пятна.
  `npm run typecheck` зелёный; на 430 и 360 нет горизонтального скролла, `main` не
  прокручивается, стыки без ступенек, лента открыта по центру ПРЕМИУМА.
  **Слайд 6 закончен, кроме блокера Q18 (номер WhatsApp) и Q23.**
- **31.08.2026.** Две грабли при переносе слайда 6, обе записаны в разделе про код:
  (1) точки под лентой не работали — `scrollTo({behavior:'smooth'})` не живёт со
  `snap-mandatory`, лечится присваиванием `scrollLeft`; (2) крайние карточки не
  доезжали до центра — проценты в `padding` считаются от содержащего блока, а лента
  на 40px шире него из-за `-mx-5`. Плюс всплыло, что Tailwind не подхватывает **новые
  файлы** без перезапуска dev-сервера, а не только новые папки: из-за этого
  `size-[13px]` не генерировался и галочки в тарифах были нулевой ширины.
- **01.09.2026, слайд 2, «слишком мрачно».** Владелец: блок «Для кого» «слишком
  мрачный, очень тёмный, как будто бы депрессивный». Подтвердилось замером: секция
  была самой тёмной на странице — верх 3.7 по яркости, земля вокруг ленты 17, при
  41 у соседнего «Автора обучения». Причина оказалась не в подборе цвета, а
  структурная: **общее световое поле `CourseBackdrop` замаскировано
  `transparent 0px → seam`, а `seam` — это нижняя граница самой «Для кого».**
  Свет на странице включается ровно там, где секция кончается, и два пятна,
  написанные под неё же (зарево на `about+32` и отсвет слева на `seam-30`),
  лежат выше маски и не показывались никогда. Подсветить секцию снизу нельзя:
  слой на `z-[3]` обёртки, а секция поднята на `z-10` — он физически под ней.
  Поэтому свет вернулся **внутрь** секции, теми же цветами, плюс два пятна по
  замеру (зелёное под кнопкой на газоне и синее в левый верх, где земля держалась
  на 11). Маска гасит его у обеих границ в ноль — иначе ступенька.
  Заодно сняты плотности: диагональ .74 → .46, шторка под лентой .74 → .52,
  тень в карточке доведена не до (3,6,9) .98, а до (11,21,34) .92 — четыре
  почти чёрные плиты подряд и делали «депрессию»; контраст не пострадал,
  белый в 85% даёт там 14.9:1.
  **Попутно найден настоящий баг на стыке с героем.** Герой гаснет в сплошной
  `#000206` (1.8), шторка стыка в «Для кого» тоже гасила в `#000206`, а
  собственный фон секции — `#0c141d` (19). Поперёк страницы стояла чёрная полоса
  в 60px, темнее **обоих** соседей: 13.7 → 1.8 → 10.2. Последний стоп героя
  переведён на `#070d15`, шторка стыка — на `rgba(2,5,9,.62)`. Стало
  17.6 → 13.2 → 10.2 → 11.3, ровный сход.
  Итог по замеру: верх 3.7 → 10.2, земля вокруг ленты 17 → 32, левое поле в
  верхней половине 11 → 24–35. Фотографии не тронуты — только контейнер и CSS.
  `npm run typecheck` зелёный; на 430 и 360 `scrollWidth == clientWidth`,
  `main` не прокручивается.
  Грабля: MCP-сервер playwright в сессии не поднялся (`CONNECTION_CLOSED`).
  Скриншоты снимались напрямую кешированным chromium из
  `~/AppData/Local/ms-playwright/chromium_headless_shell-1217` флагом
  `--screenshot`, замеры — своим декодером PNG (`scratchpad/png.py`, Pillow в
  системе нет). Геометрия и проверка на переполнение — через временный
  `__check.html` в корне: он тем же origin грузит страницу в iframe и печатает
  `scrollWidth`/`offsetHeight` в DOM, который забирает `--dump-dom`. Файл удалён.
- **01.09.2026, слайд 2, четвёртая партия кадров.** Владелец прислал `docs/111–444.png`
  взамен ночных снимков. Замер прежних объяснял, почему CSS упёрся: 18–26 средней
  яркости и 84–89% пикселей темнее 40 — темнота была в самих файлах, конвертация тут
  ни при чём (исходники `docs/1–4.png` рядом с webp дают ту же картину). Наивный
  `filter: brightness()` проверен и отвергнут: на кадре, где 89% пикселей у чёрного,
  умножение поднимает черноту, а не сюжет — кадр мутнеет.
  Новые переведены в webp по 900px и легли под теми же именами
  (`audience-academy/amateur/pro/player`), чтобы не трогать импорты: 111 → младшие
  возрасты, 222 → любительские, 333 → профессиональные, 444 → игрок в тренеры.
  `position` у всех приведён к '50% 50%' — исходник 1086×1448, под постер 268×370
  обрезается 9.5px по горизонтали и ничего по вертикали, а прежние значения были
  подобраны под людей в кадре. `alt` переписаны: старые описывали тренера с командой,
  на новых людей нет вообще.
  **Тень в карточке перевёрнута.** Она росла до .92 у нижнего края — это было верно
  для ночных кадров, ровных по яркости. Новые гаснут сами: 165–180 в верхних 45%,
  78–110 там, где начинается текст, 45–47 у низа, — и тень душила уже потушенное.
  Теперь максимум .62 на 58% и отпускание до .34 к низу. Замер контраста по колонке
  вне глифов: фон под текстом 38–70, худший контраст 7.2:1 при норме 4.5.
  Итог: средняя яркость ленты 28.6 → 96.7. `npm run typecheck` зелёный, на 430 и 360
  `scrollWidth == clientWidth`, `main` не прокручивается, высота секции те же 622px.
  **Открытый вопрос по набору (задан владельцу, ответа нет):** на всех четырёх кадрах
  нет людей — это инвентарь на поле, и различие сегментов держится только на реквизите.
  Плюс кадры 111, 222 и 444 сняты с одной точки в одном свете и в ленте почти не
  отличаются друг от друга; выделяется только 333 (большой стадион). Для карусели это
  риск: 30.08 владелец уже жаловался, что «слайды не двигаются», и тогда это была
  механика, а здесь может повториться на картинке.
  Грабля: конвертация шла через canvas в headless chromium (`toDataURL('image/webp')`),
  cwebp и ImageMagick в системе нет. Первый заход дал битые файлы — маркер вокруг
  base64 встречался и в самом тексте скрипта в DOM, и выборка забирала два совпадения.
  Лечится сборкой маркера из кусков в рантайме плюс проверкой, что размер в заголовке
  RIFF совпадает с длиной файла.
- **01.09.2026, слайд 2, кадры менялись четыре раза за сессию.** Записи выше про
  партию `111–444` **устарели** — она на странице не стоит. Итоговая съёмка нарезана
  из одного листа `docs/new.png`: четыре кадра сеткой 2×2 с белым швом. Швы найдены
  по доле белых пикселей в колонке и строке (вертикальный 557–563, горизонтальный
  697–704), каждая четверть отдана в родном разрешении 550×690 **без апскейла** —
  под постер 268×370 это чуть больше двух пикселей на точку, а растяжка до 900 дала бы
  только вес. Раскладка взята по позиции на листе (слева направо, сверху вниз), а не
  по сюжету: раскладку по сюжету владелец забраковал — «не метчится вообще».
  Промежуточные партии и чему научили:
  — `111–444`: дневные, но без людей и три кадра из четырёх почти одинаковые;
  — `1111–4444`: люди появились, но в файлы была **запечена белая рамка** (28–93px,
    у одного вместо неё тёмный кант) — с `object-fit: cover` она вылезла бы белыми
    краями. Автообрезка по опорному цвету из середины края, не из угла: у кадра с
    тёмным кантом угол чёрный, а край — уже картинка. Первая версия детектора брала
    порог «почти белое» и съела 150px настоящего бледного неба;
  — `1–4`: тот же сюжет без людей, заменены листом `new.png`.
  **Главный вывод — про тень в карточке, он записан прямо в `AudienceItem.tsx`.**
  Её форма не универсальна и следует за яркостью нижней трети кадра. За день она
  менялась четырежды вместе со съёмкой: ночным кадрам нужна была плотность внизу;
  кадрам, которые гаснут сами (45–47 у нижнего края), — наоборот отпускание книзу
  (.62 на 58%, .34 в конце); партии со светлыми тактическими досками в переднем плане
  (пики 247–253 под самым текстом) — сплошные .86–.92, иначе текст нечитаем; итоговой
  съёмке — почти прозрачная до 36% и резкий набор до .74 к 46%, где начинается текст.
  Пологий градиент здесь давал ровно 4.5:1 без запаса, потому что самое светлое место
  кадра приходится на верхний край текстового блока. Мерить надо **не среднее, а самую
  светлую точку фона в прямоугольнике текста, отдельно от глифов** — для этого кадр и
  тень рендерятся без текста.
  Итог: худший контраст 7.0:1 у основного текста и 8.9:1 у заголовков при норме 4.5;
  средняя яркость ленты 28.6 → 79.5. `alt` переписаны под новые сюжеты, `position` у
  всех '50% 50%' (по вертикали не обрезается ничего). `npm run typecheck` зелёный,
  на 430 и 360 `scrollWidth == clientWidth`, `main` не прокручивается, высота секции
  прежние 622px.
  **Осталось на владельце:** люди в кадре теперь есть у всех четырёх, но передний план
  по-прежнему инвентарь, и сегменты различаются реквизитом и планом, а не героем.
- **01.09.2026, слайд 5, три правки владельца.** «Онлайн-платформа — убрать этот
  блок. Убрать маленькую надпись „Как проходит обучение". Пункт Онлайн-платформа
  добавить перед „Команда", „обратная связь"». Сделано ровно это:
  — снят блок-герой платформы (панель со свечением по краю, окном учебного кабинета,
    списком модулей, полосой прохождения и меткой «24/7»). Вместе с ним ушла функция
    `PlatformWindow` и импорт `PROGRAM`: кроме окна модули курса этой секции незачем.
    Свечение `LightEdge` осталось только у карт бесплатных доступов — приём должен
    выделять что-то одно;
  — снята подпись первой категории. Она повторяла заголовок секции слово в слово,
    стоя на 40px ниже него. У двух других категорий подпись на месте: они называют
    то, чего в заголовке нет. Отступ `mt-8` с подписи переехал на сам список;
  — ключ `platform` переехал первым в `CATEGORIES.process.keys`, ступени стали
    01–06: платформа → команда → обратная связь → микрогруппы → практика → разборы.
  **Макетная `/learning.html` правилась отдельно.** Все три подачи там строятся
  вокруг платформы-героя, и после переезда ключа она показывалась бы дважды —
  и героем, и строкой списка. Добавлен `catKeys()`, вычитающий `platform` из
  категории; проверено по DOM: «Онлайн-платформа» встречается там столько же раз,
  сколько «Команда», то есть дублирования нет.
  Высота секции 1727 → 1485px на 430 (1630 на 360), страница короче на 242px;
  слой `CourseBackdrop` в 7200px покрывает с прежним запасом. `npm run typecheck`
  зелёный, на 430 и 360 `scrollWidth == clientWidth`, `main` не прокручивается.
  Блок-герой не потерян: он цел в `src/dev/LearningLayouts.tsx`, если понадобится
  вернуть.
- **01.09.2026, слайд 6, блок обращений.** Владелец: «„Узнать про условия для
  клубов и академий" и „Узнать про приобретение отдельных модулей" — эти пункты
  удали, вместо них сделай красивый блок с надписью „Не подошёл ни один вариант?
  Напишите в WhatsApp:" и кнопка с иконкой ватсап „Узнать про условия рассрочки"».
  Три тихие ссылки под лентой заменены одной панелью с кнопкой.
  **Правилась только главная — макетную `/tariffs.html` просили не трогать.**
  Поэтому `CONTACT_LINKS` в данных сохранён целиком (на нём стоит макетная,
  проверено по DOM: все три текста там на месте), а для главной добавлен
  `CONTACT_ACTION = CONTACT_LINKS[0]`. Текст кнопки не дублируется строкой в
  разметке: он совпадает с первым пунктом прежнего списка, и стартовое сообщение
  к нему уже написано — две копии одной строки рано или поздно разойдутся.
  Устройство: панель `rounded-[6px]` с рамкой и слабой заливкой, световая кромка,
  вопрос и под ним кнопка во всю ширину, 48px высоты (тач-цель), на 360 заголовок
  переносится в две строки, кнопка остаётся в одну (271px).
  **Про зелёный.** Это единственное место на странице с фирменным цветом WhatsApp
  (#25D366) — он тут не «ещё один акцент», а часть знака мессенджера: вместе с
  глифом читается как «написать в WhatsApp», а не как кнопка сайта. От газонного
  `.btn-hero-turf` отличается и тоном, и материалом (тот полосатый и приглушённый).
  Тарифных цветов блок не трогает намеренно — он про то, что человек не выбрал ни
  один тариф. Текст на кнопке тёмно-зелёный `#08291A`, а не белый: белый на этом
  фоне даёт 2.1:1, тёмный — 7.9:1.
  Q18 в силе: номера нет, `whatsappHref` отдаёт `undefined`, кнопка рисуется, но
  никуда не ведёт. Вид при этом оставлен боевым намеренно — приглушать её из-за
  отсутствующих данных значило бы показывать владельцу не то, что он получит.
  **Внешнее свечение** добавлено следом, по отдельной просьбе владельца. У кнопки
  два слоя: ближний `0 3px 12px rgba(37,211,102,.22)` держит контур, дальний
  `0 12px 32px rgba(37,211,102,.3)` даёт ореол — одним слоем свет либо липнет к
  краю, либо расплывается мутным пятном. Плотность дальнего слоя взята с оглядкой
  на тень ХИТа (`0 14px 36px rgba(30,91,255,.24)`): сопоставимо, но не громче,
  иначе обращение начнёт спорить с тарифом, который надо продавать. У панели своё
  широкое и слабое `0 10px 40px rgba(37,211,102,.10)` — оно работает опорой, а не
  украшением: сразу под блоком секция гаснет в `bg-pitch`, и без ореола нижняя
  грань рамки растворялась вместе с фоном. С панели при этом пришлось снять
  `overflow-hidden` — он обрезал собственную тень. Замер спада под кнопкой:
  (18,81,46) вплотную и (5,8,11) через 50px, то есть ровное затухание без обрыва.
  `npm run typecheck` зелёный, на 430 и 360 `scrollWidth == clientWidth`,
  `main` не прокручивается.
- **01.09.2026, слайд 8 (слайд 7 обойдён по указанию владельца).** Заведены
  `src/data/faq.ts` (семь вопросов и ответов дословно из ТЗ; правки только
  типографские — «ё» и «ёлочки»), макетная `/faq.html` с тремя подачами
  (`src/dev/FaqLayouts.tsx`, точка входа `src/faq.tsx`). Живую страницу не
  трогали: до выбора владельца `App.tsx` без изменений.
  **Механику выбирать не из чего** — ТЗ задаёт её целиком: «вопрос делаем
  видимым, ответ нужно, чтобы раскрывался, если человек нажмет на +».
  Поэтому во всех трёх подачах аккордеон и плюс, который становится минусом
  схлопыванием вертикального штриха; различается материал: 01 волосяные
  линии, 02 плашки «Программы», 03 Bebas-капслок с нитью у раскрытого.
  Общее для всех трёх: открыт ровно один вопрос (первый — сразу, как в
  «Программе»), номеров у вопросов нет, высота раскрытия анимируется по
  `ResizeObserver`.
  **Замер в браузере** (высота секции с одним раскрытым ответом, 430 / 360):
  01 — 566 / 627, 02 — 652 / 683, 03 — 555 / 632; `scrollWidth == clientWidth`
  во всех шести рамках. Первая прикидка «01 самая низкая, 03 самая высокая»
  замером не подтвердилась и переписана прямо на макетной: 01 и 03 идут
  вровень, выше обоих 02. Bebas узкий — крупный кегль забирает ширину, а не
  высоту.
  **Своего браузерного инструмента в сессии не было** (MCP playwright не
  поднялся), снимки и замеры сделаны headless-хромиумом из кеша playwright
  через CDP — скрипт лежит в скретчпаде сессии, в репозиторий не попал.
  `npm run typecheck` зелёный. **Ждём выбора владельца.**
  Что переносить вместе с выбранной подачей: сход в `bg-pitch` снять с
  «Выбери свой тариф» и поставить FAQ (он всегда у последней секции),
  дотянуть цветные пятна `CourseBackdrop` ниже `prog+4400`, завести якорь
  `#faq`.
- **01.09.2026, слайд 8 перенесён в боевую.** Владелец выбрал подачу 03
  «Крупная полоса»: «3 вариант берем, только адаптируй под background».
  Заведена `src/components/FaqSection.tsx`, встала последней секцией в
  `App.tsx`. Сход в `bg-pitch` снят с «Выбери свой тариф» и переехал в FAQ —
  третий переезд этого слоя, он всегда живёт у последней секции.
  **Про background — то, ради чего правка и просилась.** Цветное поле
  `CourseBackdrop` кончалось на `prog+4400`, а секция занимает
  `prog+4285…4892` на 430 и `prog+4682…5366` на 360, то есть почти целиком
  лежала на голом тоне: «переливание» обрывалось ровно на последней секции —
  та же болезнь, что была у слайда 6. Добавлено одно синее пятно с центром
  `prog+4700` (компромисс между двумя ширинами: на 430 приходится на низ
  секции, на 360 на её середину, радиус 560 накрывает обе), плотность .22
  против тарифных .26. Синий — цвет нити у раскрытого вопроса, единственный
  цвет секции; золото сюда не идёт, оно этажом выше замыкает шкалу тарифов и
  второй тёплый круг подряд читался бы продолжением тарифов.
  Слой поднят 7200 → 7600px: обёртка выросла до 6117 на 430 и 6637 на 360,
  и на 360 под раскрытые аккордеоны оставалось всего 160px.
  **Замеры в браузере** (430 и 360): `scrollWidth == clientWidth`, `main` не
  прокручивается, стык тарифов с FAQ без ступеньки, низ гаснет в подвал
  ровно — по краске 17,29,44 → 5,8,11 за 140px и дальше плоский `bg-pitch`;
  тон под секцией поднят пятном с базовых 10,17,25 до 17,29,44. Аккордеон
  проверен нажатием через CDP: третий вопрос раскрылся, первый закрылся.
  `npm run typecheck` зелёный. Макетная `/faq.html` оставлена со всеми тремя
  подачами.

- **01.09.2026, слайд 9.** Слайд был заблокирован по Q20 («контента нет»), и
  разблокировал его владелец, а не заказчик: текст цитаты взят с работающего
  `gadjiev.pro` («Профессия тренера — творческая…»), кадр назван там же —
  `coach-band.webp`, широкая полоса 1100×511 из первой партии, лежавшая в
  ассетах неиспользуемой с 31.08, когда слайд 3 переехал на кадр в полный
  рост. Заведены `src/data/quote.ts` (текст дословно, ни слова не дописано),
  макетная `/quote.html` с тремя подачами (`src/dev/QuoteLayouts.tsx`, точка
  входа `src/quote.tsx`). Живую страницу не трогали — `App.tsx` без
  изменений, до выбора владельца.
  **Выбирать тут надо не содержимое, а роль секции на странице:** текст и
  кадр заданы. 01 «Кадр и слова» — портрет сверху, цитата под ним во всю
  ширину; 02 «Слова на кадре» — приём слайда 3, текст в левой тёмной части
  подложки; 03 «Закрывающий кадр» — слова по центру, под ними кадр, гаснущий
  в подвал.
  **Замер высоты секции (430 / 360):** 01 — 476 / 528, 02 — 400 / 400,
  03 — 606 / 632.
  **Грабля, стоившая половины сессии:** у подачи 02 не применялся
  `min-h-[400px]`, хотя класс в файле был, а в CSS по `?direct` — тоже. В
  системе висели пять dev-серверов прошлых сессий (5173–5176), новый сел на
  5177, а браузер смотрел на старый 5173 с протухшим кешем Tailwind:
  страница отдавала CSS без новых утилит. Лечится только тем, чтобы убить
  старые процессы `vite` и поднять сервер заново — `netstat -ano | grep 517`
  показывает их разом. Проверять адрес в логе `npm run dev` перед замерами.
  **Почему у 02 вообще нужна `min-h`:** кадр растягивается по высоте секции,
  и на высоте, которую задаёт один текст (178px), полоса 1100×511 срезается
  по бокам вчетверо — фигура выезжает на середину экрана и ложится ровно под
  цитату. На 400px срез вдвое меньше, фигура остаётся справа, как в файле.
  Своего браузерного инструмента в сессии снова не было (MCP playwright не
  поднялся), снимки и замеры — headless-хромиумом из кеша playwright через
  CDP, скрипт в скретчпаде сессии.
  `npm run typecheck` зелёный. **Ждём выбора владельца.**
- **01.09.2026, слайд 9, второй заход.** Первые три подачи владелец отклонил
  целиком: «ни один вариант мне не нравится, хочу чтобы слева текст справа
  ГМ». Общее у всех трёх было то, что текст и снимок стояли друг над другом
  (или текст лежал поверх кадра) — геометрия колонок не примерялась вовсе.
  Заведены две подачи в заданной геометрии: **01 «Портрет в вылет»** (колонка
  вплотную к правому краю, растворение масками 44px слева и 90px снизу,
  подпись прижата к низу текстовой колонки) и **02 «Портрет карточкой»**
  (скруглённый бокс с волосяной рамкой и полем справа, подпись под самим
  кадром). Три отклонённые остались внизу макетной для сравнения.
  **Колонка кадру помогает.** Полоса `coach-band.webp` во всю ширину давала
  голову 51px; в колонке 46% при высоте 300px кадр покрывает бокс по высоте
  (масштаб 0.587), в окно попадает 337px исходника из 1100, якорь 77% ставит
  туда ровно фигуру — голова 76px, пустой левый угол снимка срезается целиком.
  **Высота секции (430 / 360):** 01 — 300 / 315, 02 — 342 / 342. Обе вдвое
  ниже отклонённых (476…632).
  Маска у 01 двухслойная, поэтому нужен `mask-composite: intersect` плюс
  вебкитовский `-webkit-mask-composite: source-in`; величины растворения в
  пикселях, не в процентах — иначе на 360 и на 430 оно разной толщины.
  `npm run typecheck` зелёный. **Ждём выбора владельца.**
- **01.09.2026, слайд 9 перенесён в боевую.** Владелец выбрал подачу «Врезка
  с обтеканием» из третьего захода. Заведена
  `src/components/QuoteSection.tsx`, встала последней секцией в `App.tsx`.
  **Три захода до выбора, восемь отклонённых подач.** Первый: текст и кадр
  друг над другом (кадр и слова, слова на кадре, закрывающий кадр) — «ни
  один вариант мне не нравится, хочу чтобы слева текст справа ГМ». Второй:
  кадр колонкой рядом с текстом и кадр фоном блока (во всю ширину и
  карточкой) — «пересмотри вообще этот блок, давай его сделаем полностью
  другим». Третий заход сделан со скиллом `ui-ux-pro-max`: стиль
  `editorial-grid-magazine` дал врезку с обтеканием, буквицу и печатные
  линейки вместо рамок, пара «Bold Typography Mobile» — приём type-as-hero
  для отклонённой подачи «Манифест».
  **Обтекание сняло вопрос, об который спотыкались все восемь подач:** где
  стоит фотография относительно колонки текста. Колонка рядом со снимком
  была узкой на всю высоту блока и цитата в 215 знаков шла в ней десятью
  строками; при обтекании узкие только первые четыре строки.
  **Отдельная правка после переноса — подгонка к FAQ.** Первая боевая
  версия повторяла макетную: карточка с волосяной рамкой и скруглением,
  цитата 16px, своя линейка byline. Владелец: «вообще не метчится с
  блоком „Часто задаваемые вопросы“, как будто бы просто вкинули блок». Он
  прав структурно: ниже тарифов на странице нет ни одной рамки, FAQ
  построен на волосяных линиях, капслоке и синей нити. Материя секции
  заменена на его: волосяная линия `white/[.07]` сверху, синяя нить 2px
  слева с тем же затуханием и отступом `pl-3.5`, имя набрано ровно как
  вопрос (Bebas 17px, трекинг 1px, `#EAF2FF`), у врезки сняты рамка и
  скругление, края растворяются масками (40px слева, 56px снизу), кадр ушёл
  в вылет к правому краю и приглушён до 0.86.
  **Фон.** Сход в `bg-pitch` снят с FAQ и переехал в цитату — четвёртый
  переезд слоя, он всегда у последней секции. В `CourseBackdrop` добавлено
  пятно 13 (`prog+5250`, плотность .16, синее): секция занимает
  `prog+4893…5212` на 430 и `prog+5366…5695` на 360, и на узком экране
  целиком оказывалась ниже цветного поля — та же болезнь, что была у
  слайдов 6 и 8. Плотность ниже справочных .22 у FAQ: у самой секции
  холодный кадр с прожекторами, второе синее пятно в полную силу спорило бы
  с ним. Слой поднят 7600 → 7800px.
  **Грабля сессии:** текст в двух подачах рисовался ПОД шторками кадра.
  Абсолютный слой рисуется поверх непозиционированного содержимого, и блоку
  с текстом нужен `relative` — в подаче «Финальный кадр» цитата пропадала
  целиком. Вторая: замеры `offsetTop` в этой вёрстке считаются от обёртки
  секций, а не от документа (у обёртки `position: relative`), и первые
  замеры краски были сняты на 800px выше нужного места — мерить надо
  `getBoundingClientRect().top + scrollY`.
  **Замеры в браузере** (430 и 360): `scrollWidth == clientWidth`, `main` не
  прокручивается, стык FAQ → цитата без ступеньки (18,31,47 → 18,30,46),
  низ гаснет в подвал ровно (14,24,36 → 9,16,25 → 5,8,12 → плоский
  `bg-pitch`). Высота секции 319 / 329.
  `npm run typecheck` зелёный. Макетная `/quote.html` оставлена со всеми
  девятью подачами.
- **01.09.2026, слайд 9 снят со страницы.** Через несколько минут после
  переноса владелец сказал «убери пока этот блок». Откачено ровно три вещи,
  и все три — обратимые: `<QuoteSection />` убрана из `App.tsx`, сход в
  `bg-pitch` вернулся в FAQ (он всегда у последней секции страницы), из
  `CourseBackdrop` снято пятно 13 и высота слоя вернулась с 7800 к 7600.
  Сама секция, данные и макетная остались в репозитории — так же в нём
  лежат `AboutSection` и `LeadSection`. Как вернуть, записано в разделе
  «Текущий слайд».
  **Замер после снятия** (430): низ страницы снова гаснет ровно —
  22,36,54 → 15,25,36 → 6,9,13 → плоский `bg-pitch`, стык с подвалом без
  ступеньки. `npm run typecheck` зелёный.
- **01.09.2026, юридический прогон страницы.** Владелец попросил проверить
  сайт на соответствие законам РФ и поставить в подвал нужные ссылки.
  Заведены `src/data/legal.ts` и юридический блок в `SiteFooter`: волосяная
  линия FAQ, ссылки на документы, строка реквизитов, и только потом «Создано
  с любовью». Тач-цель ссылок 44px по высоте.
  **Пока документов нет, блок не врёт:** ссылка без `href` рисуется
  приглушённой надписью, строка реквизитов не выводится, пока не заполнено ни
  одно поле `SELLER`. Тот же приём, что у кнопок WhatsApp в тарифах без
  номера. Битая ссылка на «Политику» хуже её отсутствия — она обещает
  документ, которого нет.
  **Что на странице сейчас нет и что это упрощает:** форм нет (LeadSection
  снята), счётчиков и cookie-баннеров нет, кнопки оплаты — заглушки, номера
  WhatsApp нет. То есть прямо сейчас страница персональные данные не
  собирает; всё перечисленное становится обязательным ровно в тот момент,
  когда включают оплату или ставят форму.
  **Разбор по закону — в `03-QUESTIONS.md`:** Q24 переписан (политика ПД по
  152-ФЗ, оферта, реквизиты по ЗоЗПП, уведомление в РКН, локализация базы в
  РФ, чеки по 54-ФЗ, чекбоксы согласий в будущей форме), добавлены **Q25**
  (лицензия на образовательную деятельность: тексты обещают проверку домашних
  заданий «членами команды», и от схемы оформления зависят формулировки на
  странице) и **Q26** (согласие на использование изображения ГМ по ст. 152.1
  ГК, права на логотипы NANOFOOTBALL и SCOUTWAY, совпадение рекламных
  обещаний с офертой).
  Тексты самих документов не сочиняем — это работа юриста заказчика, и
  правило «тексты не сочиняем» здесь работает тем более.
  `npm run typecheck` зелёный, проверено на 430 и 360.
- **01.09.2026, мини-подвал.** Владелец: «сделай ссылки красивыми, сделай
  мини footer для сайта». Подвал из одной строки превращён в четыре яруса,
  разделённые волосяными линиями `white/[.07]` — теми же, что делят вопросы
  FAQ. Рамок подвал не заводит: ниже тарифов их на странице нет.
  1. **Название академии** — дословно строка первого экрана («Онлайн-академия
     футбольных тренеров Гаджи Гаджиева»), ничего не сочинено;
  2. **навигация по шести секциям** в две колонки, Bebas-капслок 13px. Это
     единственная навигация на странице — шапки в макете нет, и с низа
     страницы вернуться к тарифам было нечем. Подписи — сокращённые заголовки
     самих секций; «Автор обучения» взят из надзаголовка слайда 3, потому что
     заголовок там — имя. Все шесть якорей проверены: каждый находит свою
     секцию;
  3. **правовой ярус** — документы Onest 11.5px с подчёркиванием и строка
     реквизитов;
  4. **нижняя строка** — «© 2026. Все права защищены» слева и подпись студии
     справа. Год берётся с часов, а не вписан числом.
  **Ссылки различаются намеренно.** Навигация — Bebas, её жмут чаще всего;
  документы — Onest с подчёркиванием, потому что подчёркивание здесь обещает
  документ. Пока документа нет, надпись приглушена и подчёркнута **пунктиром**:
  пунктир говорит «готовится», сплошная линия обещала бы работающую ссылку.
  Контакт «Написать в WhatsApp» готов и появится сам, как только в
  `data/tariffs.ts` появится номер (`whatsappHref`).
  **Заодно снят мёртвый экран внизу:** у `<main>` было `pb-28` — запас от
  липкой кнопки, которой в макете нет. Пока подвал был одной строкой, пустота
  читалась воздухом; под подвалом с навигацией она стала пустым экраном.
  Теперь `pb-8`.
  **Замеры в браузере** (430 и 360): горизонтального скролла нет, `main` не
  прокручивается, высота подвала 411 и 435px, минимальная тач-цель среди всех
  ссылок подвала — 44px. `npm run typecheck` зелёный.
- **01.09.2026, два бага навигации.** Владелец: переходы по кнопкам идут без
  анимации, и при нажатии на «Посмотреть тарифы» пропадает блок, на котором
  эта кнопка стоит.
  **Первый — не было плавной прокрутки вообще.** `scroll-behavior` в проекте
  не стоял нигде, все переходы (`#program`, `#tariffs`, ссылки подвала)
  прыгали мгновенно, и прыжок на три экрана читался перезагрузкой. Добавлено
  в `index.css`: `html { scroll-behavior: smooth }` под
  `prefers-reduced-motion: no-preference` и `section[id] { scroll-margin-top:
  16px }` — иначе заголовок секции упирался в самую кромку экрана.
  **Второй оказался не «пропаданием блока», а прокруткой обёртки внутри
  себя.** Слой `CourseBackdrop` висел в обёртке секций своей полной высотой
  7600px, а обёртка была 6117 — `overflow-hidden` прячет полосу прокрутки, но
  прокручиваемости не убирает, и `scrollHeight` был больше `clientHeight`.
  Браузер по якорю прокручивает **каждого** прокручиваемого предка цели,
  поэтому нажатие «Посмотреть тарифы» двигало и страницу, и саму обёртку —
  замер: `wrap.scrollTop` уходил в 1483px. Секции внутри неё уезжали вверх
  под обрез, и выглядело это как исчезнувший блок.
  Ровно та же грабля уже была в проекте: когда-то слой делал прокручиваемым
  `<main>`, и лечили её как раз добавлением `overflow-hidden` на обёртку —
  то есть проблему передвинули на уровень ниже, а не убрали.
  **Лечение:** корень `CourseBackdrop` стал клипующей коробкой `inset-0` по
  размеру обёртки, а слой фиксированной высоты 7600px переехал внутрь неё.
  Обёртке больше нечего прокручивать (`scrollHeight == clientHeight`),
  фиксированная высота слоя осталась при нём — на ней держалась
  производительность при раскрытии аккордеона.
  **Проверено кликами через CDP** по всем шести якорям (`#audience`,
  `#coach`, `#program`, `#learning`, `#tariffs`, `#faq`): `wrap.scrollTop`
  остаётся 0, цель встаёт в 16px от верха экрана, все семь секций на месте.
  Краска фона после перестройки прежняя (22,37,54 → 16,26,37 → 6,9,13 →
  плоский `bg-pitch`). `npm run typecheck` зелёный.
- **01.09.2026, чистка репозитория.** Владелец: «макет готов, удалим полностью
  ненужные файлы, страницы html и прочий мусор». Перед удалением сделан
  коммит-снимок `0f33445` — из него восстанавливается всё, что ниже.
  **Удалено:**
  — девять макетных страниц (`audience`, `buttons`, `coach`, `faq`, `learning`,
    `program`, `quote`, `tariffs`, `variants`) вместе с точками входа
    `src/*.tsx` и каталогом `src/dev/` (все подачи слайдов, черновик
    программы);
  — серверная часть: `api/`, `db/`, `scripts/migrate.mjs`, `tsconfig.api.json`,
    `.env.example`, `LOCAL_DEVELOPMENT.md`, форма заявки (`LeadSection`,
    `Field`, `usePhoneMask`, `lib/phone.ts`), зависимости `pg`, `drizzle-orm`,
    `@types/pg`, `@vercel/node`, плагин локального API в `vite.config.ts` и
    команда `npm run db:init`;
  — снятые со страницы компоненты: `AboutSection`, `SiteHeader`,
    `QuoteSection` вместе с `data/quote.ts`, хук `useReveal`;
  — 37 неиспользуемых ассетов (19 МБ): png-исходники рядом с webp, золотые
    бейджи регалий `1–6`, кадры отклонённых подач слайда 3
    (`coach-portrait`, `coach-portrait-side`, `coach-scene`, `coach-band`),
    картинки снятой секции «О курсе» (`course-*`), четвёртая партия кадров
    `111–444`;
  — исходники и скриншоты: `docs/*.png|jpg`, `docs/tz.docx`,
    `docs/newassets/`, `docs/ASSETS-PROGRAM.md`, `docs/NEXT-SECTIONS.md`,
    снимки экрана в корне, каталоги `dist/`, `.playwright-mcp/`,
    `.vercel-cli-cache/`;
  — мёртвая ссылка на Manrope из Google Fonts в `index.html` — шрифт на
    странице не использовался, а запрос к CDN висел на каждой загрузке.
  **Оставлено намеренно:** вся документация `docs/tz/*.md` и `docs/HANDOFF.md`
  (без неё следующая сессия начинается с нуля — это точка входа по CLAUDE.md),
  `scripts/to-webp.mjs` с зависимостью `sharp` (конвертер кадров заказчика в
  WebP, к бэкенду отношения не имеет), `public/` с иконками.
  **Комментарии в коде поправлены там, где они стали врать:** `App.tsx`
  (карта страницы), `AudienceSection` и `index.css` больше не обещают, что
  снятые компоненты «остались в репозитории». Ссылки на `/*.html` в остальных
  комментариях оставлены как исторические — они объясняют, откуда взялась
  подача.
  **Проверка после чистки:** `npm run typecheck` и `npm run build` зелёные
  (бандл 254 КБ / 78 КБ gzip), в браузере на 430 и 360 горизонтального скролла
  нет, `main` не прокручивается, все шесть якорей работают, подвал на месте.
- **02.09.2026, лист оплаты (вне очереди слайдов).** Владелец: «нам нужно
  сделать 2 модальных окна для кнопок „Оплатить полностью“ и „Забронировать“,
  модалки должны отличаться по цветам, в модальных окнах будет информация по
  оплате, сразу берётся тот тариф который выбрал пользователь».
  Сессия 01.09 успела заложить только основание и оборвалась на лимите: в
  `data/tariffs.ts` появился `RESERVE_AMOUNT`, в `data/faq.ts` — `faqAnswer()`,
  в `icons.tsx` — `CloseIcon`, в `index.css` — весь CSS листа. Компонента не
  было, кнопки по-прежнему отвечали заглушкой «Скоро», и всё перечисленное
  лежало мёртвым кодом. 02.09 достроено.
  **Что сделано:** `src/components/PaySheet.tsx` — нижний лист на модальном
  `<dialog>`; состояние открытого листа поднято в `TariffsSection` (один лист
  на секцию, а не три копии в ленте); `TariffCard` и `PayButtons` получили
  `onPay`; заглушка Q19 переехала на главную кнопку листа; в `index.css`
  добавлен замок прокрутки `body:has(.pay-dialog[open])`.
  **Содержимое листа:** панель тарифа (название в цвете ступени) + суммы
  («К оплате» для полной оплаты, «Вносится сейчас» и «Остаток» для брони,
  остаток считается) + дословный ответ FAQ + главная кнопка с подписью из
  таблицы заказчика. Своего текста только служебные подписи интерфейса.
  **Грабля, которую поймали заранее:** уход листа ждёт `animationend`, а под
  `prefers-reduced-motion` анимация обнулена и событие не приходит вовсе —
  лист не закрылся бы никогда. В `requestClose` стоит проверка `matchMedia`:
  кому анимации выключены, диалог закрывается сразу.
  **Проверено headless-хромиумом через CDP** на 430 и 360: оба листа у всех
  трёх тарифов открываются с верными суммами (49 900 / 64 900 / 99 900 и
  остатки 39 900 / 54 900 / 89 900), грань шапки берёт цвет действия
  (`rgb(159,180,204)` / `rgb(106,160,255)` / `rgb(255,193,74)` против
  `rgb(33,179,101)` у брони), закрытие работает крестиком, Esc и тапом мимо,
  `body` запирается на время показа и отпускается после, горизонтального
  скролла нет, ошибок в консоли нет, заглушка отвечает «Скоро».
  `npm run typecheck` и `npm run build` зелёные (бандл 257,7 КБ / 78,6 КБ gzip).
- **02.09.2026, преимущества тарифа в листе.** Владелец: «внеси красивую
  информацию в модалке по поводу главных преимуществ тарифа». В `PaySheet`
  добавлен блок `Advantages` между суммами и условиями оплаты: подпись
  «Главное в тарифе», список прибавки ступени (значок в плашке 30px в цвете
  тарифа + название Bebas-капслоком + описание из слайда 5) и строка «Плюс
  всё, что входит в тариф „…“» для ПРЕМИУМА и ВИПа.
  Данные целиком существующие, ни одного нового текста: `addedIn` +
  `MATRIX_FOLDED` дают состав, `learningItem` — название, описание и значок.
  Разбор, почему прибавка, а не весь состав, и почему у `feedback` описание
  скрыто, — в шапке компонента и в таблице решений выше.
  **Что поправлено по отрисовке:** значок бонусного модуля (поле → мяч, он
  повторял «Практику») и линия под шапкой листа.
  **Проверено через CDP** на 430 и 360, три сочетания тариф×действие:
  списки верные (5 / 6 / 3 позиции), описания стоят у семи пунктов из
  четырнадцати, значки в каждом списке различны, лист упирается в потолок
  88dvh (820px при 932) и прокручивается внутри себя, подвал с кнопкой
  остаётся на месте, горизонтального скролла нет, консоль чистая.
  `npm run typecheck` и `npm run build` зелёные (бандл 260,0 КБ / 79,2 КБ
  gzip).
- **02.09.2026, лист покупки: форма, способы оплаты, три преимущества.**
  Владелец: «мне не нравится. Сделай 3 штуки преимуществ на каждом тарифе.
  Также пересмотри ui. Ещё нам нужны будут поля ввода mail, telegram (если
  есть), whatsapp (если есть). А также номер телефона (обязательно). Сверху
  способы оплаты (prodamus), снизу форма, снизу чекбоксы с политикой и
  обработкой данных.»
  **Порядок блоков задан им же** и идёт сверху вниз: итог (тариф и сумма) →
  главное в тарифе (три позиции) → способы оплаты и строка про Prodamus →
  контактные данные (четыре поля) → согласия → подвал с кнопкой и ссылкой
  на оферту. Итог и преимущества стоят выше способов намеренно: сначала
  человек убеждается, что выбрал то, что хотел.
  **Новые файлы:** `src/components/PayForm.tsx` (поля, согласия, проверка),
  `src/lib/phone.ts` и `src/hooks/usePhoneMask.ts` (подняты из `0f33445`
  без единой правки), значки `MailIcon`, `TelegramIcon`, `LockIcon`.
  **Данные:** `HIGHLIGHTS` + `highlightsOf`, `PAY_METHODS`,
  `PAY_METHODS_NOTE`, `PAY_PROVIDER` в `data/tariffs.ts`; `legalDoc()` в
  `data/legal.ts`.
  **CSS:** мёртвый набор `.fi-*` заменён на `.pay-*`; цвет фокуса полей и
  залитых чекбоксов приезжает переменной `--pay-accent`, которую лист
  ставит инлайном, — форма окрашена тем же действием, что кнопка под ней.
  **Устройство кнопки.** Она стоит в подвале, вне области прокрутки, и
  связана с формой атрибутом `form="pay-form"`: подвал остаётся приклеенным
  к нижней кромке экрана, а нажатие всё равно запускает проверку полей.
  Обернуть `<form>` вокруг прокрутки и подвала было нельзя — на этой тройке
  (шапка / прокрутка / подвал) держится флекс-раскладка листа.
  **Что поправлено по отрисовке:** у поля в фокусе рисовались два
  индикатора разом — цветная рамка коробки и белое кольцо
  `.pay-sheet :focus-visible` поверх неё; кольцо снято с полей и чекбоксов,
  у остального осталось.
  **Проверено через CDP** на 430 и 360, с настоящими событиями ввода:
  списки по три позиции на каждом тарифе, пять плашек способов оплаты,
  маска собрала `+7(999)123-45-67` из голых цифр, пустая отправка подняла
  сводку с тремя строками и зажгла обе обязательные ошибки, фокус уехал в
  сводку (`activeElement` с `role=alert`), верная отправка показала строку
  про Prodamus, правка почты после успеха эту строку сняла, кривой адрес на
  blur зажёг ошибку под своим полем, кегль поля 16px, горизонтального
  скролла нет ни на одной ширине, консоль чистая. `npm run typecheck` и
  `npm run build` зелёные (бандл 271,7 КБ / 82,5 КБ gzip).
  **Не сделано и почему:** форма никуда не отправляется — бэкенда в проекте
  нет по условию, приём оплаты идёт этапом 10.
- **02.09.2026, лист покупки переписан начисто (третий заход).** Владелец:
  «полностью не нравится как выглядит форма и блоки, полностью с нуля
  переделай», затем — «просто нужна нормальная стандартная форма, с
  красивой подачей, посмотри как это сделано на таких же сайтах».
  **Три концептуальные подачи (билет с линией отрыва, табло стадиона,
  протокол матча) были предложены и отвергнуты** — метафора здесь не
  нужна вовсе. Вывод на будущее: лист оплаты — утилитарный интерфейс, и
  языком витрины его собирать нельзя.
  **Что смотрели.** Общие правила чекаута (поля в зоне большого пальца,
  6–8 полей, цели 44px+, ранняя честность про суммы) и порядок самого
  Prodamus: сайт показывает заказ и собирает контакты, карта вводится на
  его стороне. Это и зафиксировало роль листа.
  **Что стало:** сводка заказа обычными строками «что / сколько» с итогом
  под чертой (у брони строк больше — стоимость, вносится сейчас, остаток);
  «что входит» — простой список с галочками без рамок и капслока;
  «способ оплаты» — одна строка-панель с замком, именем сервиса и
  перечислением способов; контактные данные — четыре поля с подписями
  обычным текстом; согласия; подвал с кнопкой «Оплатить 64 900 ₽».
  **Переписаны целиком** `PaySheet.tsx` и блок `.pay-*` в `index.css`,
  перенабрана типографика `PayForm.tsx`. Поведение формы (проверка на blur,
  сводка ошибок, фокус в неё, маска телефона, два согласия) не менялось —
  оно уже было верным, менялась только подача.
  **Проверено через CDP** на 430 и 360: заголовки блоков читаются
  («Что входит в тариф», «Способ оплаты», «Контактные данные»), по три
  позиции на тариф, маска собрала `+7(999)123-45-67`, пустая отправка
  подняла сводку и зажгла обе обязательные ошибки, верная показала строку
  про Prodamus, правка после успеха её сняла, кегль поля 16px,
  горизонтального скролла нет, консоль чистая. `npm run typecheck` и
  `npm run build` зелёные (бандл 270,9 КБ / 82,3 КБ gzip).
