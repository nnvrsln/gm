/**
 * «Как проходит обучение?» — слайд 5 ТЗ.
 *
 * Тексты дословные из `docs/tz/01-SPEC.md`, раздел «Слайд 5». Правка ровно
 * одна и типографская: расставлена «ё» («развёрнутая», «подтверждёнными») —
 * весь сайт ёфицирован, так же сделано в `audience.ts`. Слова не трогали.
 *
 * **Одиннадцать пунктов отвечают на три разных вопроса, а не на один.**
 * В ТЗ они идут единым списком, но по смыслу это:
 *   — как устроены занятия (платформа, практика, команда);
 *   — что происходит с человеком внутри потока (обратная связь, микрогруппы,
 *     разборы с Гаджи Муслимовичем) — ядро, за это платят;
 *   — что остаётся после (доступы к NANOFOOTBALL и SCOUTWAY, сертификат,
 *     портфолио, сообщество).
 * Поэтому пункты лежат здесь плоским списком с ключами, а группировка —
 * отдельно: разных раскладок группировка разная, а тексты одни и те же.
 *
 * Порядок в `LEARNING_ITEMS` — порядок ТЗ, его не трогаем: он источник.
 * `LEARNING_TZ_SCREENS` — разбивка самого ТЗ (там пункты разделены пустыми
 * строками) плюс просьба заказчика «на каждый слайд по 2-3 фишки обучения».
 *
 * Девять из одиннадцати пунктов повторяются на слайде 6 строками тарифной
 * таблицы. Когда дойдём до тарифов — брать текст отсюда, а не набирать
 * заново: разошедшиеся формулировки человек читает как разные вещи.
 *
 * TODO (Q14): заказчик просил «визуально отразить то, о чём мы говорим» —
 * то есть 11 иллюстраций, которых нет. Пока в слоте картинки стоит
 * контурная иконка из общего набора проекта.
 *
 * TODO (Q15): NANOFOOTBALL и SCOUTWAY — сторонние продукты. Нужны их
 * логотипы и подтверждение, что мы вправе их показывать.
 */
import type { ComponentType, SVGProps } from 'react'
import {
  CertificateIcon,
  ConstructorIcon,
  FeedbackIcon,
  LockedChatIcon,
  MicrogroupIcon,
  PlatformIcon,
  PortfolioIcon,
  ReviewIcon,
  ScoutIcon,
  SoccerFieldIcon,
  TeamIcon,
} from '../components/icons'

export type LearningItem = {
  /** Ключ для раскладок: они группируют пункты по-разному. */
  key: string
  /** Название фишки капслоком — как в ТЗ. */
  title: string
  /** Пояснение под названием, дословно из ТЗ. */
  text: string
  icon: ComponentType<SVGProps<SVGSVGElement>>
  /**
   * Сторонний продукт: NANOFOOTBALL и SCOUTWAY. Им нужна своя подача —
   * это не свойство курса, а выданный доступ, и слот под логотип (Q15).
   */
  partner?: boolean
}

export const LEARNING_ITEMS: LearningItem[] = [
  {
    key: 'platform',
    title: 'Онлайн-платформа',
    text: 'уроки в записи на удобной образовательной платформе в доступе 24/7',
    icon: PlatformIcon,
  },
  {
    key: 'feedback',
    title: 'Обратная связь',
    text: 'качественная и развёрнутая обратная связь в чатах и при проверке домашних заданий',
    icon: FeedbackIcon,
  },
  {
    key: 'practice',
    title: 'Практика',
    text: 'вы сразу будете переносить полученные знания на поле и проверите их в работе со своей командой',
    icon: SoccerFieldIcon,
  },
  {
    key: 'team',
    title: 'Команда',
    text: 'с вами будут работать профессионалы с подтверждёнными результатами в футболе',
    icon: TeamIcon,
  },
  {
    key: 'microgroups',
    title: 'Разработка тренировок в микрогруппах',
    text: 'будете не просто подбирать упражнения, а аргументированно строить тренировку под конкретную игровую задачу и защищать свои решения перед коллегами',
    icon: MicrogroupIcon,
  },
  {
    key: 'nanofootball',
    title: 'NANOFOOTBALL',
    text: 'получите бесплатный доступ к цифровой среде для планирования тренировочного процесса с упражнениями, видео, анимацией, конструктором тренировок, календарём и статистикой',
    icon: ConstructorIcon,
    partner: true,
  },
  {
    key: 'scoutway',
    title: 'SCOUTWAY',
    text: 'получите бесплатный доступ к платформе для футбольных скаутов и спортивных клубов, которая предназначена для поиска, анализа и управления информацией об игроках',
    icon: ScoutIcon,
    partner: true,
  },
  {
    key: 'reviews',
    title: 'Разборы тренировок',
    text: 'каждая группа вместе с Гаджи Муслимовичем будет анализировать, насколько удалось выполнить поставленную цель, какой результат получить, а также разбирать отдельные упражнения',
    icon: ReviewIcon,
  },
  {
    key: 'certificate',
    title: 'Сертификат',
    text: 'каждый участник получит именной сертификат, подтверждающий прохождение программы и освоение методов футбольной подготовки',
    icon: CertificateIcon,
  },
  {
    key: 'portfolio',
    title: 'Тренерское портфолио',
    text: 'к концу обучения у вас будет готовое тренерское портфолио, которое можно использовать при трудоустройстве, общении с руководителями клубов и презентации собственной методики',
    icon: PortfolioIcon,
  },
  {
    key: 'community',
    title: 'Закрытое тренерское сообщество',
    text: 'каждый участник получит доступ в закрытый канал для футбольных тренеров, где можно задавать вопросы, обсуждать игровые и тренировочные ситуации, делиться опытом и вместе с коллегами находить практические решения даже после завершения обучения',
    icon: LockedChatIcon,
  },
]

/** Пункт по ключу. Падает громко: опечатка в ключе — это ошибка раскладки. */
export function learningItem(key: string): LearningItem {
  const found = LEARNING_ITEMS.find((item) => item.key === key)
  if (!found) throw new Error(`Нет пункта обучения с ключом «${key}»`)
  return found
}

/** Несколько пунктов в заданном порядке — раскладки собирают группы сами. */
export function pickLearning(...keys: string[]): LearningItem[] {
  return keys.map(learningItem)
}

/**
 * Разбивка самого ТЗ: там одиннадцать пунктов разделены пустыми строками на
 * четыре части — 3, 2, 3 и 3. Секция на ней не стоит (владелец отверг ленту
 * по этой разбивке: она делит пункты по месту в списке, а не по смыслу), но
 * оставлена как справка по источнику.
 */
export const LEARNING_TZ_SCREENS: LearningItem[][] = [
  pickLearning('platform', 'feedback', 'practice'),
  pickLearning('team', 'microgroups'),
  pickLearning('nanofootball', 'scoutway', 'reviews'),
  pickLearning('certificate', 'portfolio', 'community'),
]

/**
 * Категории секции — то, на чём она стоит сейчас.
 *
 * Деление придумано не нами и не «по логике»: владелец задал его прямо —
 * «разобьём на категории основные действия, к примеру как проходит
 * обучение, потом что мы получаем бесплатно». Одиннадцать пунктов ТЗ идут
 * единым списком, но отвечают на три разных вопроса, и это ровно они.
 *
 * ОНЛАЙН-ПЛАТФОРМА раньше не входила ни в одну категорию: она была героем
 * секции и стояла отдельным блоком выше всех — по требованию владельца
 * «обязательно нужно выделить, что обучение будет проходить на
 * онлайн-площадке». 01.09 владелец это отменил: блок-герой снят, а пункт
 * переехал первым в категорию «процесс», перед КОМАНДОЙ и ОБРАТНОЙ СВЯЗЬЮ.
 * Макетные подачи в `src/dev/LearningLayouts.tsx` по-прежнему строятся вокруг
 * платформы-героя, поэтому там этот ключ вычитается из категории обратно.
 *
 * Цвета. Синий — акцент всей страницы. Мята под «бесплатно» выбрана
 * холодной: тёплый зелёный занят кнопками `.btn-hero-turf`, и карта доступа
 * в нём выглядела бы кнопкой. Золото на странице уже означает «сверх
 * основного» — им помечен бонусный модуль в «Программе обучения», и на
 * сертификате с портфолио оно означает то же самое.
 *
 * TODO: названий категорий в ТЗ нет — это единственный текст слайда 5 не из
 * ТЗ, он ждёт согласования заказчиком.
 */
export type LearningCategory = {
  id: string
  label: string
  /** Цвет метки, номера ступени и рамки карты. */
  color: string
  keys: string[]
}

export const CATEGORIES: LearningCategory[] = [
  {
    // label этой категории на странице больше не рисуется: 01.09 владелец
    // попросил снять «маленькую надпись „Как проходит обучение"» — она
    // повторяла заголовок секции слово в слово. Поле оставлено, потому что
    // тип общий на три категории, а у двух других подпись на месте.
    id: 'process',
    label: 'Как проходит обучение',
    color: '#6AA0FF',
    // Порядок внутри категории — как это проживается за неделю: заходите на
    // платформу → с вами работают профессионалы → они отвечают на домашнее
    // задание → вы строите тренировку в микрогруппе → выносите её на поле
    // своей команды → разбираете результат с Гаджи Муслимовичем.
    // ПЛАТФОРМА стоит первой по прямому указанию владельца от 01.09:
    // «пункт Онлайн-платформа добавить перед „Команда", „обратная связь"».
    keys: ['platform', 'team', 'feedback', 'microgroups', 'practice', 'reviews'],
  },
  {
    id: 'free',
    label: 'Что получаете бесплатно',
    color: '#3FE0B0',
    keys: ['nanofootball', 'scoutway'],
  },
  {
    id: 'after',
    label: 'Что остаётся после',
    color: '#FFC14A',
    keys: ['certificate', 'portfolio', 'community'],
  },
]
