import heroPhoto from '../assets/hero-gadzhiev.webp'

export function Hero() {
  return (
    // Низ берётся из .section-rhythm (32px): у кнопок свечение слабое
    // (блюр 20px против прежних 30px) и успевает погаснуть до стыка, не
    // подкрашивая чёрный синим. Верх свой — это первый экран, а не рядовая
    // секция, и 168px отмеряют место под шапку кадра — на 36px больше
    // исходных 132px, чтобы текстовый блок не залезал на лицо ГМ в кадре.
    // Фото не трогаем: img внутри абсолютно спозиционирован и от этого
    // отступа не зависит, сдвигается только контент в потоке.
    <section className="section-rhythm relative isolate overflow-hidden pt-[168px]">
      {/* Снимок альбомный (1337×1176), контейнер узкий — кадр увеличен до 178%
          и сдвинут влево, чтобы в окно попали фигура справа, прожектор в правом
          верхнем углу и газон снизу, а левая половина осталась под текст. */}
      <img
        src={heroPhoto}
        alt="Гаджи Гаджиев на стадионе"
        className="photo-sharp-down pointer-events-none absolute -top-[10px] left-[-6%] z-0 w-[112%] max-w-none select-none brightness-[1.16] saturate-[1.06]"
      />
      {/* Размытая копия того же кадра: подхватывает низ там, где резкая уже
          погасла. filter применяется до маски, поэтому blur не «вытекает». */}
      <img
        src={heroPhoto}
        alt=""
        aria-hidden="true"
        className="photo-blur-down pointer-events-none absolute -top-[10px] left-[-6%] z-0 w-[112%] max-w-none select-none brightness-[1.16] saturate-[1.06]"
      />

      {/* Затемнение слева — под заголовок, снизу — стык со следующей секцией.
          Держим его слабым: фон должен читаться стадионом, а не чёрной плашкой. */}
      <div className="absolute inset-0 z-[1] bg-[linear-gradient(100deg,rgba(5,8,11,.74)_0%,rgba(5,8,11,.26)_56%,transparent_94%)]" />
      {/* Фото гаснет в чистый чёрный ДО конца секции — последние ~35px это
          ровная чёрная полоса. Замер макета: над стыком там rgb(0,1,5), и
          именно эта пауза делает границу со следующим блоком читаемой. */}
      <div className="absolute inset-x-0 bottom-0 z-[2] h-60 bg-[linear-gradient(180deg,transparent_0%,rgba(3,5,8,.32)_46%,rgba(0,2,6,.86)_84%,#000206_100%)]" />

      <div className="relative z-10 px-5">
        {/* ── Название академии ─────────────────────────────────────────────
            ТЗ (docs/tz/01-SPEC.md, слайд 1): вместо «АВТОР КУРСА / Гаджи
            Гаджиев» — одна строка с новым именем. Она длиннее прежней в разы
            (52 знака против 13), поэтому кегль и трекинг уменьшены против
            исходной «Автор курса»: на всю фразу трекинг --tracking-course
            (0.18em) растянул бы её на 3–4 рваные строки. */}
        <p className="max-w-[300px] font-badge text-[11px] font-bold uppercase leading-[1.4] tracking-[.04em] text-white/58">
          Онлайн-академия футбольных тренеров{' '}
          {/* Имя выделено без нового цвета (красный не используем нигде на
              странице) — просто полная непрозрачность и вес жирнее общего
              текста строки, тот же приём, что делает разницу между title и
              описанием в остальных секциях. */}
          <span className="font-extrabold text-white">Гаджи Гаджиева</span>
        </p>
        {/* TODO(docs/tz/03-QUESTIONS.md, Q1/Q2): это не автограф Гаджиева, а
            латиница шрифтом Autograf (лицензия personal use only). Стоит,
            пока не пришлют скан настоящей подписи или решение её убрать. */}
        <span className="signature mt-2.5 block text-[29px] leading-none">G. Gadzhiev</span>

        {/* ── Заголовок ──────────────────────────────────────────────────── */}
        {/* Новое продуктовое обещание длиннее прежнего в разы (78 знаков
            против 26), тем же 48px оно не встаёт. Кегль снижен до 32px и
            трекинг взят section-title'овский (--tracking-title, 1px) — на
            48px он был откалиброван под другой размер. Переносы расставлены
            вручную по фразовым группам, а не автопереносом: капслок Bebas на
            рваной правой границе читается плакатом, а не абзацем.

            Отступы вокруг блока и leading заголовка отпущены шире, чем
            минимально нужно для посадки текста: меньший кегль сам по себе
            сжал весь блок по высоте против прежней версии (48px в 3 строки),
            воздух здесь возвращает первому экрану прежний вертикальный
            масштаб. */}
        <h1 className="hero-title mt-5 text-[32px] uppercase leading-[0.96] tracking-title">
          <span>Научитесь системно</span>
          <span>выстраивать</span>
          <span>тренировочный процесс</span>
          <span>и управлять развитием</span>
          <span>своей команды</span>
        </h1>

        <p className="mt-5 max-w-[340px] text-[12.5px] font-medium leading-[1.55] text-white/72">
          Практическая система подготовки от Гаджиева Гаджи Муслимовича: управление нагрузкой,
          подбор упражнений, развитие индивидуального мастерства и моделирование игры
        </p>

        {/* ── Кнопка ─────────────────────────────────────────────────────── */}
        {/* ТЗ: кнопка одна, ведёт на программу. Была пара — вторая (btn-hero-secondary)
            снята, оставшаяся растянута на всю ширину: половина строки без соседа
            читалась бы обрубком. */}
        <div className="mt-9 flex">
          <a href="#program" className="btn-hero btn-hero-primary w-full">
            Узнать программу
          </a>
        </div>
      </div>
    </section>
  )
}
