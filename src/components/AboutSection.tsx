import courseBg from '../assets/course-bg.webp'

/**
 * Со страницы снята (30.08.2026). Несла обложку слайда 2 — заголовок
 * «ДЛЯ КОГО?» на фотографии стадиона отдельной секцией, и под заголовком
 * оставалось 240px пустого кадра. Обложка переехала внутрь AudienceSection,
 * там же теперь живут снимок и все шторки. Компонент оставлен в репозитории
 * как CoachSection и LeadSection: вернуть его — значит вписать строку в
 * App.tsx, но тогда `id="about"` и заголовок начнут дублировать
 * AudienceSection.
 */
export function AboutSection() {
  return (
    // Стык с героем — встречное растворение: герой гаснет в чёрный снизу,
    // секция проявляется из того же чёрного сверху. Границы как таковой нет.
    <section id="about" className="section-rhythm relative overflow-hidden bg-[#080d12] px-5">
      {/* Кадр прижат вправо и увеличен до 140%: так прожектор попадает в
          правый верх, а газон дотягивается до нижней части секции. При 112%
          снимок кончался на 361px и низ секции оставался пустым. */}
      <img
        src={courseBg}
        alt=""
        aria-hidden="true"
        className="photo-sharp-up pointer-events-none absolute right-0 top-0 z-0 w-[140%] max-w-none select-none brightness-[1.44] saturate-[1.14]"
      />
      {/* Размытая копия перехватывает верх: стадион проявляется из темноты
          сначала расфокусированным и только потом наводится на резкость. */}
      <img
        src={courseBg}
        alt=""
        aria-hidden="true"
        className="photo-blur-up pointer-events-none absolute right-0 top-0 z-0 w-[140%] max-w-none select-none brightness-[1.44] saturate-[1.14]"
      />
      {/* Плотность всех трёх слоёв снижена: кадр был затемнён до того, что
          стадион не читался вовсе, а секция выглядела чёрной плитой с
          пятном. Снимок поднят с brightness 1.24 до 1.44, диагональная
          шторка с .88 до .74, нижняя — с .92 до .7. Ниже этих значений
          опускаться нельзя: строки списка идут прямо по каркасу ворот.
          Затемнение слабеет книзу: газон в нижней части кадра должен остаться
          зелёным, а не уйти в общий тон панели.
          Тон — тот же почти нейтральный rgba(5,8,11), что в герое. Раньше
          здесь стоял rgba(0,18,29): в канале синего +29 против +11 в красном,
          и весь кадр уходил в бирюзу — рядом с живым первым экраном секция
          читалась как другой сайт. По той же причине saturate держится
          низким (1.3 → 1.14): множитель тянул не столько зелень газона,
          сколько синеву в тенях трибун. */}
      <div className="absolute inset-0 z-[1] bg-[linear-gradient(100deg,rgba(5,8,11,.74)_0%,rgba(5,8,11,.26)_54%,rgba(5,8,11,.02)_100%)] [mask-image:linear-gradient(180deg,#000_0%,#000_32%,rgba(0,0,0,.34)_66%,rgba(0,0,0,.18)_100%)] [-webkit-mask-image:linear-gradient(180deg,#000_0%,#000_32%,rgba(0,0,0,.34)_66%,rgba(0,0,0,.18)_100%)]" />

      {/* База панели (#080d12) светлее чёрного, в который гаснет герой, —
          без этого слоя на стыке видна ступенька. Лежит под контентом
          (z-[2] против z-10), поэтому заголовок не притухает. */}
      <div className="pointer-events-none absolute inset-x-0 top-0 z-[2] h-20 bg-[linear-gradient(180deg,#000206_0%,rgba(0,2,6,.55)_42%,transparent_100%)]" />

      {/* Шторка под содержимым: на газоне и на каркасе ворот текст теряет
          контраст. Стопы в пикселях от верха, не в процентах, — высота секции
          на узких экранах гуляет из-за переносов, а зона перехода должна
          стоять на месте. Маска по горизонтали отпускает шторку справа:
          нижний правый угол кадра с освещённым газоном — единственное живое
          зелёное пятно, гасить его целиком нельзя.

          Числа подбирались под прежнее наполнение секции (заголовок в две
          строки, абзац и список опор курса, начинавшийся на ~217px). Сейчас
          здесь заголовок в одну строку и два кадра сегментов — если на
          стыке фотографий полезет грязь, править надо эти стопы, а не
          карточки. */}
      <div className="pointer-events-none absolute inset-0 z-[2] bg-[linear-gradient(180deg,transparent_0px,transparent_112px,rgba(5,8,11,.26)_176px,rgba(5,8,11,.62)_224px,rgba(5,8,11,.7)_290px)] [mask-image:linear-gradient(90deg,#000_0%,#000_62%,rgba(0,0,0,.72)_88%,rgba(0,0,0,.55)_100%)] [-webkit-mask-image:linear-gradient(90deg,#000_0%,#000_62%,rgba(0,0,0,.72)_88%,rgba(0,0,0,.55)_100%)]" />

      {/* Секция стала обложкой блока «Для кого?»: заголовок на фотографии
          стадиона, а сегменты — лентой в AudienceSection следом. Разнести их
          иначе нельзя: карусели нужен один трек на все четыре карточки, между
          двумя секциями её не разрезать.

          min-h нужен, чтобы снимку хватило места. Кадр спозиционирован
          абсолютно и обрезается высотой секции — на одном заголовке она
          схлопнулась бы до ~120px, и от стадиона осталась бы верхняя полоска,
          которая по маске ещё даже не проявилась. */}
      <div className="relative z-10 flex min-h-[236px] flex-col">
        {/* ТЗ (docs/tz/01-SPEC.md, слайд 2): «Убираем "для кого этот курс?" и
            "этот курс для вас, если вы?", пишем просто: ДЛЯ КОГО?» — поэтому
            здесь нет ни надзаголовка, ни второй строки. Заголовок один на обе
            секции: они стоят на общем свете CourseBackdrop, границы между
            ними не видно, и повторять его во второй секции незачем. */}
        <h2 id="audience-title" className="section-title text-[32px] uppercase leading-[.94] tracking-title">
          Для кого?
        </h2>
      </div>
    </section>
  )
}
